## 1. Метрики

- [x] 1.1 Добавить в `RenderMetrics` поля `dimOffsetPx`, `dimOvershootPx`, `dimGapPx`, `dimArrowPx`, `dimTextGapPx` со значениями для `SCREEN_METRICS` (px) и `PDF_METRICS` (мм листа), удалить `labelGapPx`; проверить `npm run build`

## 2. Внутренняя сторона

- [x] 2.1 Добавить в `geometry.ts` функцию определения стороны размера (чётность пересечений лучей по нормалям с осями остальных стен, правило полуинтервала; фолбэк при неоднозначности); юнит-тесты в `geometry.test.ts`: замкнутый квадрат — внутрь, Г-образный контур — внутрь, незамкнутая цепочка — фолбэк, перегородка между двумя комнатами — фолбэк; `npm test`

## 3. Отрисовка размера

- [x] 3.1 Заменить `drawLengthLabel` на отрисовку размера (размерная линия, залитые стрелки, выносные с зазором и выступом, число над линией с белой подложкой; локальные координаты стены и нормализация угла сохраняются), использовать новую сторону из 2.1; превью — серым; `npm run build`
- [x] 3.2 Убрать суффиксы единиц из `formatLength` («1907», «191», «1,5»); обновить проверки формата в тестах; `npm test`

## 4. Проверка

- [x] 4.1 Браузерная проверка: замкнутая комната — размеры внутри на любом зуме; незамкнутая стена — фолбэк-сторона; превью при рисовании; `npm run dev`
- [x] 4.2 Проверить PDF-экспорт: размерные элементы в миллиметрах листа, не зависят от масштаба чертежа

## 5. Число размера — длина оси (уточнение 2026-09-05)

- [x] 5.1 В `render.ts`: якоря выносных — концы оси стены, число = `Math.hypot(A, B)`; `npm run build`
- [x] 5.2 Удалить из `geometry.ts` `wallFace` (якоря «в свету» зависели от соседних стен) и её тесты; `npm test`

## 6. Сторона при перегородках (исправление 2026-09-05)

- [x] 6.1 `dimensionSide`: голосование соседей в вершинах (знак `cross(u, far−E)`), чётность лучей — только тай-брейк при равенстве голосов; регресс-тест «комната с перегородкой через середину»; `npm test && npm run build`
- [x] 6.2 Браузерная проверка: комната с перегородкой — все размеры на внутренних гранях, без «прибавленной толщины»

## 7. Стабильность при удалении (исправление 2026-09-05)

- [x] 7.1 Число размера зависит только от самой стены (длина оси, соседи не участвуют): `npm test && npm run build`
- [x] 7.2 Браузерная проверка: удаление перегородки не меняет размеры наружных стен
