## 1. Типы и миграция

- [x] 1.1 `src/types.ts`: новые `WALL_TYPES` (bearing/selfbearing/nonbearing, подписи, `hatchSpacingCm`/`contourPx`) и `normalizeWallType`; юнит-тесты нормализации (partition/drywall → nonbearing, bearing → bearing, неизвестное → nonbearing) проходят
- [x] 1.2 `src/storage.ts`: применять `normalizeWallType` в `parseStore` к обоим форматам; тест миграции (черновик со старыми типами парсится в nonbearing/bearing) проходит

## 2. Отрисовка по ГОСТ

- [x] 2.1 `src/render.ts`: контур + векторная штриховка 45° по параметрам типа (clip-полигон, шаг в мировых см, контур в экранных px), fallback неизвестного типа в стиль nonbearing; превью с альфой и циановое выделение сохраняются
- [x] 2.2 `index.html` / `src/style.css`: свотчи подпанели — монохромные мини-штриховки (CSS) с рамкой по весу контура; дефолтный `wallType` в `main.ts` — nonbearing

## 3. Верификация

- [x] 3.1 В браузере: три стены разных категорий различимы (плотность+контур) на разных зумах; дефолт «Ненесущая»; стрелки ↑/↓ с зацикливанием (в т.ч. при закрытой панели); смена типа меняет стиль только выделенной; симуляция старого localStorage-чертежа открывается как «Ненесущая»
- [x] 3.2 `npm test` и `npm run build` — зелёные
