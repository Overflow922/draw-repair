## 1. Модель данных и версии

- [x] 1.1 Добавить `Wall.id` (uuid) в `types.ts`; назначать при создании в `commitPoint`, копировать в `cloneWalls`; обновить `isWall`/`isDrawing`; прогнать `npm test` — существующие тесты правок стен зелёные
- [x] 1.2 Добавить `DimEnd`, `Dimension`, `Drawing.dimensions` в `types.ts` и валидатор `isDimension`; тест: валидация корректного и битого размера
- [x] 1.3 Перевести историю на сценные снимки `{walls, dimensions}` (history.ts v2): `record`/`recordSnapshot`/undo/redo меняют обе ссылки в main.ts; история v1 при загрузке сбрасывается; тесты: roundtrip снимка с размерами, сброс v1
- [x] 1.4 Хранилище v3 (`storage.ts`): `dimensions` в `Drawing`, парсинг v1–v3, миграция v1/v2 (id стенам, `dimensions: []`), readOnly при `> 3`; тесты: открытие v1, v2, v3, будушая версия, первое сохранение пишет v3

## 2. Рендер и удаление авто-логики

- [x] 2.1 Переписать `drawDimension` на модель (A, B из привязок, P1/P2 через offset); удалить `dimensionSide`, `axisCrossings`, авто-цикл в `drawScene` и preview-размер; удалить тесты `dimensionSide`; тест: геометрия точек P1/P2/выносных
- [x] 2.2 Прогнать `npm test` и `npm run build`: приложение рендерит стены без размеров, ошибки типов и консоли отсутствуют

## 3. Инструмент «Размер»

- [x] 3.1 Добавить кнопку и состояние `tool: "dimension"` (тулбар, `syncToolUI`, сбросы в `setTool`/`activate`); клик по стене в этом режиме не выделяет стену; проверка в браузере: активация, Escape в «none»
- [x] 3.2 Реализовать размещение: шаги 1–2 через `hitWall` + проекция t в [0,1], шаг 3 — offset; черновик `dimDraft`, превью в `drawScene`, Esc/dblclick отмена, нулевой размер не создаётся; тесты: проекция и зажим t, вычисление offset
- [x] 3.3 Запись размещения в историю (сценный снимок) и undo/redo; проверка в браузере: постановка, Ctrl+Z, Ctrl+Y, перезагрузка страницы (размер восстановлен)

## 4. Правки, удаление, drag

- [x] 4.1 Следование размеров за стенами: рендер уже выводится из модели — покрыть тестами (перемещение, растяжение конца, поворот оси) и проверить один шаг истории при жесте
- [x] 4.2 Каскадное удаление: `deleteWall` вычищает размеры по `wallId` одним `record`; ластик находит размеры (хит-тест отрезок+текст), подсветка, ближайший объект; тесты: каскад, хит-тест
- [x] 4.3 Перетаскивание размерной линии: drag-стейт, offset из курсора, один шаг истории на жест; тесты и проверка в браузере
- [x] 4.4 Магнитная привязка точек замера к вершинам стен: `vertexAt` с приоритетом над hitWall (клик рядом с концом внутри стены даёт t=0|1); тесты vertexAt, browser-проверка
- [x] 4.5 Прикрепление точек замера к углам стен: `dimEndAt` возвращает ближайший конец оси (t=0|1, дробные точки исключены); спека/design обновлены; тесты и browser-проверка
- [x] 4.6 Прямые углы без митры: `endCap` — митра только для непрямых углов (|cos φ| ≥ sin 5°), 90° — примыкание прямоугольниками; дельта спеки wall-joints, тесты, browser-проверка
- [x] 4.7 Превью цепочки примыкает к предыдущей стене: `drawWall` превью получает `[...walls, preview]` (стык как у зафиксированной стены, без наложения тел); дельта спеки wall-joints «Свободные торцы и превью», browser-проверка
- [x] 4.8 Утапливание оси новой стены в тело предыдущей на полтолщины (`jointPullback` в commitPoint/превью): дальняя грань новой полосы проходит через конец оси предыдущей, угол закрыт без выемки, габарит предыдущей не меняется; тесты, browser-проверка
- [x] 4.9 Точки замера — пересечения линий чертежа: модель `EdgeRef {wallId, edge 0..3}` + `DimPoint {a, b}` (`edgeLine`, `dimPointPoint`), привязка через `nearestEdgeIntersection`; `DimEnd`/`dimEndAt`/`vertexAt`/`dimEnds` удалены; снисходящий парсинг хранилища (битые/устаревшие размеры отбрасываются, стены сохраняются); дельты спек dimension-tool/drawing-storage и design обновлены; тесты и browser-проверка

## 5. PDF и финал

- [x] 5.1 BBox с размерами: P1/P2 и запас под текст в `wallsBBox`; тест: размер за габаритом сужает список форматов; проверка экспорта: размеры в PDF, черновик не попадает
- [x] 5.2 Полный прогон: `npm test`, `npm run build`, браузерная проверка сценариев спек (размещение/следование/ластик/drag/экспорт); `openspec validate manual-dimensions`
