## 1. Разметка и стили

- [x] 1.1 Перестроить `index.html`: холст на всё окно, острова — вкладки сверху, инструменты слева (колонка кнопок + подпанель «Стены» правее с толщиной и типами, id сохранены); проверить отсутствие ошибок в консоли dev-сервера
- [x] 1.2 `style.css`: общий класс `.panel` из рецепта `#size-box`, позиции островов (12px от краёв, кнопки по центру вертикали), видимость подпанели по модификатору, `#size-box` и вкладки переведены на `.panel`; проверить визуально: границы блоков заканчиваются на содержимом, углы скруглены, ничего не тянется до краёв окна

## 2. Логика подпанели (`main.ts`)

- [x] 2.1 Toggle панели кликом по кнопке «Стена»; авто-открытие при выделении стены; снятие выделения не меняет видимость; ручное закрытие держит панель закрытой до нового выделения; проверить все четыре сценария в браузере
- [x] 2.2 Проверить в браузере: стрелки ↑/↓ переключают тип при закрытой панели; стрелки в поле толщины меняют значение нативно; рисование, выделение, undo/redo, вкладки работают как прежде

## 3. Верификация

- [x] 3.1 Прогнать сценарии спеков дельты `canvas-app` в браузере (острова, отступы, клики по блокам не создают элементов) и дельты `wall-types` (образцы, стрелки, дефолт)
- [x] 3.2 `npm test` и `npm run build` завершаются успешно; регрессий логики нет
