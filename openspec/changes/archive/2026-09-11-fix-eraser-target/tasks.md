## 1. Фикс

- [x] 1.1 В `eraserTarget` (src/main.ts) ограничить размерную ветку радиусом привязки (`dd <= tol`); приоритет «размер поверх стены» сохранить. Проверка: существующие тесты зелёные (`npm run test`), tsc/build зелёные

## 2. Верификация

- [x] 2.1 Браузером: курсор/клик далеко от размера в пустом месте — подсветки нет, ничего не удалено; в радиусе привязки — подсветка есть, клик удаляет, undo возвращает; конфликт стена+размер под курсором — приоритет размера; дев-сервер оставить запущенным, ссылку в отчёт
