## 1. Модель истории (src/history.ts)

- [x] 1.1 Типы (HistoryStore: histories, trash, version), двухстековые undo/redo над снимками Wall[] с капом 50 и FIFO-вытеснением, срез future при новом действии; unit-тесты в vitest покрывают undo/redo, срез ветки повтора, вытеснение старейших — `npm test`
- [x] 1.2 Сериализация/парсинг: валидация структуры по образцу `storage.test.ts`, битый JSON и чужая структура → пустая история, версия > 1 → режим без перезаписи; unit-тесты на все три случая — `npm test`

## 2. Хранение

- [x] 2.1 `loadHistory`/`saveHistory` для ключа `draw-repair:history` рядом с существующим `storage.ts`, сохранение привязать к существующему dirty-циклу; проверить отсутствие ключа на чистой установке — `npm run dev`, пустые стеки при первом запуске

## 3. Интеграция шагов в main.ts

- [x] 3.1 Границы жестов: снимок на `pointerdown` (wallMove/endpointDrag), одна запись на `pointerup` при фактическом изменении; проверка: перетаскивание стены откатывается одним undo с примкнутыми стенами
- [x] 3.2 Поля: толщина, длина (`resizeSelected`), тип — шаг при каждом фактическом изменении значения; проверка: изменение толщины откатывается одним undo
- [x] 3.3 Клик цепочки (`commitPoint`): шаг на каждую добавленную стену; проверка: клик-стена откатывается одним undo
- [x] 3.4 Действия undo/redo: замена `drawing.walls`, сброс `selectedWall`/`chainStart`/активных жестов (ресинк как в `activate`), блокировка во время жеста, пометка dirty; проверка: undo во время цепочки прерывает её и снимает выделение

## 4. Закрытие и корзина

- [x] 4.1 `closeDrawing`: чертёж вместе с историей в trash, запись о закрытии в стек ставшего активным чертежа; undo достаёт из корзины, вставляет на прежний индекс и активирует; проверка: закрытие активной вкладки, фоновой вкладки и единственной вкладки (автосозданный пустой остаётся) — возврат отменой в каждом случае

## 5. UI

- [x] 5.1 Кнопки «Отменить»/«Повторить» в `index.html`, disabled-стиль в `style.css`, обновление доступности после каждого изменения стеков; проверка: кнопки неактивны на пустых стеках и после среза ветки повтора
- [x] 5.2 Горячие клавиши Ctrl+Z / Ctrl+Shift+Z / Ctrl+Y в обработчике keydown; проверка: работают как кнопки, в том числе во время цепочки

## 6. Верификация

- [x] 6.1 `npm test` зелёный, `npm run build` (tsc + vite) проходит
- [x] 6.2 Ручная проверка в браузере: рисование, драги, поля, вкладки, перезагрузка страницы (истории восстановлены), сценарий «N отмен + новая стена» (повтор неактивен), сценарий «отмена закрытия»
