# Tasks: miter-wall-joints

## 1. Геометрия стыков (geometry.ts)

- [x] 1.1 Добавить константы диапазонов (30°/135°/лимит 2·толщины) и кластеризацию вершин (общие точки через `pointsEqual`, детект T-конца на оси с проекцией строго внутри); юнит-тесты кластеризации в `geometry.test.ts`
- [x] 1.2 Реализовать линию среза углового стыка (2 стены в вершине): пересечения наружных и внутренних граней, обе стены — по одной линии, разная толщина поддерживается; тесты: 90° (диагональ), 45°, разная толщина
- [x] 1.3 Реализовать bevel для θ < 30° (общая линия на лимите от вершины) и плоские торцы для θ > 135°, коллинеарных, 3+ стен в вершине и численной дегенерации; тесты: острый угол без шипа, излом 150°, 180°, три стены в точке
- [x] 1.4 Реализовать T-подрезку: срез торца приходящей стены по грани сквозной со стороны подхода; тесты: T под 90° (заподлицо) и под 30°
- [x] 1.5 Собрать `wallShape(wall, walls): Point[]` — упорядоченный полигон 4–6 вершин, свободные торцы плоские; тесты: одиночная стена = прежний прямоугольник, инвариант «полигон внутри полосы ось ± t/2»

## 2. Отрисовка (render.ts)

- [x] 2.1 Заменить `wallPolygon` на `wallShape` для стен (`drawWall`); превью остаётся плоским; проверить `npm run build` и что цепочки рисуются со срезами
- [x] 2.2 Якорь фазы линейной штриховки через текущий трансформ от мировой точки (шаг `HATCH_STEP` не меняется); проверить `npm test` и визуально: линии кирпича продолжаются через стык, при панораме не «плывут»
- [x] 2.3 Расширить диапазон волн `woodLong` за пределы оси на толщину с обрезкой клипом; проверить визуально: нет пустого клина у среза деревянного угла

## 3. Интеграционная проверка

- [x] 3.1 `npm test` и `npm run build` зелёные; все новые тесты стыков проходят
- [x] 3.2 Проверка в браузере (dev-сервер): нарисовать цепочки со стыками 90°/45°/острым/тупым, T-стык, стены разной толщины; undo/redo не меняет оси; сохранить/открыть — стыки воспроизводятся; подписи длин осевые; в отчёте дать ссылку на страницу

## 4. Правки по feedback (щель на тупых, острые углы)

- [x] 4.1 Убрать верхнюю границу митры (тупые изломы режутся митрой — щели клином больше нет) и заменить bevel острых углов на примыкание впритык: сквозная (ранняя) стена — полный прямоугольник, поздняя — прямоугольный торец, подрезанный по её грани; общий путь с T-подрезкой; обновить тесты (150°/170° — митра, 20° — примыкание)
- [x] 4.2 Перепроверить в браузере тупой излом (нет щели) и острый угол (прямоугольные блоки, тело доходит до стыка); `npm test` + `npm run build` зелёные
- [x] 4.3 Подсветка выделения повторяет срезанную форму стены (не выходит за стыки); маркеры остаются на осях; проверено в браузере

## 5. Слияние стен одного типа (feedback 2026-08-30, референс AutoCAD)

- [x] 5.1 `sameTypeJoint` в geometry.ts: поиск соседа вынесен в общий `jointAt` (использует и `endCap`); слияние = тот же материал + та же толщина при митре/коллинеарности/T-стыке; острое примыкание (φ < порога) — без слияния; юнит-тесты в `geometry.test.ts`
- [x] 5.2 `strokeContour` в render.ts: слитые торцы не рисуются (разомкнутый контур, lineCap square), разные типы — замкнутый контур как раньше; `npm test` + `npm run build` зелёные
- [x] 5.3 Проверка в браузере: 90° одного типа без шва, разные типы — шов, Т-стык одного типа, острый угол; в отчёте дать ссылку на страницу

## 6. Допуск вершины стыка (feedback 2026-08-30, «один угол не соединился»)

- [x] 6.1 `jointAt` → `Joint {c, cEnd, vertex, corner}`: угол при расхождении концов ≤ max(h)/2, каноническая вершина — конец более ранней стены; `endCap`/`buttCap` от вершины; T — дальше порога от концов; юнит-тесты (смещённый угол, коллинеарный зазор, 3 конца в допуске)
- [x] 6.2 `npm test` + `npm run build` зелёные
- [x] 6.3 Браузер: воспроизвести угол с концом на оси у торца — выемки нет; чертёж пользователя рендерится правильно; ссылка на страницу

## 7. Разные типы — прямое примыкание (feedback 2026-08-30, «не по диагонали»)

- [x] 7.1 `endCap`: митра только при том же типе (материал + толщина); разные типы при любом угле ≥ порога — примыкание впритык (ранняя — плоский торец, поздняя — `buttCap`); обновить тесты (разная толщина/материал под 90° — прямые торцы, без диагонали)
- [x] 7.2 `npm test` + `npm run build` зелёные
- [x] 7.3 Браузер: угол 90° кирпич+бетон — прямой шов без диагонали; ссылка на страницу
