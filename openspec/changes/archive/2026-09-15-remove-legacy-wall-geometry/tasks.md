# Tasks: remove-legacy-wall-geometry

Спецификации не меняются (skip_specs); поведенческий контракт — утверждённые тесты `test-suite.md` (CLN-GREP-1, CLN-COVER-1…4, CLN-TSC-1, CLN-VITEST-1) и валидация `test-validation.md` (VERDICT: PASS).

## 1. Удаление мертвого кода

- [x] 1.1 Повторная проверка живости перед удалением (design D2): grep каждого из 11 имён (`orthoAxis`, `findVertexSnap`, `snapVertex`, `handleAt`, `moveWall`, `pointOn`, `sameTypeJoint`, `wallDisplayPolys`, `pointInConvex`, `subtractCovered`, `distToSegment`) по `src/**` вне `geometry.ts`/`geometry.test.ts`/чекера. Если у какого-то символа нашёлся прод-вызов (main.ts, render.ts, wall-geometry.ts, export/) — исключить его из удаления, остановиться и зафиксировать отклонение в отчёте задачи. Тест: test-plan, Boundary Cases. — **Отклонений нет: все 11 имён встречаются только в geometry.ts/geometry.test.ts.**
- [x] 1.2 Удалить из `src/geometry.ts` мертвые экспорты по списку violations CLN-GREP-1 (11 имён из 1.1), включая непубличный `distToSegment`. Не трогать (proposal «Не трогается», design D3): `lockedDirection`, `jointAt`, `wallShape`, цепочку `endCap`/`buttCap`/`capOnFaces`, `jointTol`, `jointedWalls`, `bodyGap`, `snap` (с инлайн-орто), `moveEndpoint`, `moveWalls`, `endpointAt`, `distanceToWall`, `dimPointPoint`, `nearestEdgeIntersection`, `dimGeometry`, `dimensionOffsetAt`, `dimLevelSnap`, `dimHitDistance`, `zoomAt`, `visibleWorld`, `pointsEqual`, `segmentIntersectsRect`, `snapOthers`, реэкспорт `hitWall`. Тест: CLN-GREP-1. — **751→492 строк; вместе с `wallDisplayPolys` удалены умершие хелперы `clipHalf`/`isEarlierWall` (аналог `distToSegment`); 0 вставок в диффе.**
- [x] 1.3 Привести `src/geometry.test.ts` в соответствие (ровно по списку, ничего сверх): удалить describe-блоки `handleAt` (:155), `moveWall` (:213), `snapVertex` (:336), `sameTypeJoint` (:450), «полигоны отрисовки и заливка клина» (:507), `subtractCovered` (:535), `pointOn` (:566); убрать импорты удалённых символов. Единственный разрешённый перенос в живых тестах: кейс «направление от грани фиксируется точно перпендикулярно» (:407-415) — целиком, без правок тела, в новый `describe("lockedDirection")`. Тесты: CLN-GREP-1, CLN-COVER-4. — **939→680 строк; перенос выполнен (тело кейса не входило ни в один old_string правок); сняты 2 корреляционных ассерта sameTypeJoint в живом wallShape-тесте (F2, :897-898).**

## 2. Верификация

- [x] 2.1 `npx vitest run`: все зелёные — CLN-GREP-1 и CLN-COVER-4 перешли из красных в зелёные, CLN-COVER-1/2/3 остались зелёными (CLN-VITEST-1). — **7 файлов, 179 passed / 0 failed.** Уточнение счётчика: в tasks/test-suite стояло «216» — это доп-число ДО удаления; 37 удалённых мертвых тестов давали 216 − 37 = 179. Арифметическая описка планирования (класс F6′), на гейты не влияет: ноль упавших — и есть CLN-VITEST-1.
- [x] 2.2 `npx tsc --noEmit`: чисто — `noUnusedLocals` ловит забытые импорты удалённых символов (CLN-TSC-1). — **TSC OK.**

## 3. Дифф-ревью (чеклист R3 из test-validation.md)

- [x] 3.1 Дельта соответствует якорям объёма: `geometry.ts` ≈ −226 строк (существенно меньше — подозрение на переименование вместо удаления, мутация F4); `geometry.test.ts` ≈ −300 строк; `wall-geometry.ts`, `wall-geometry.test.ts`, `main.ts`, `render.ts` — 0 изменений. — **Факт: geometry.ts −259 (с учётом clipHalf/isEarlierWall — больше якоря, не меньше), geometry.test.ts −263/+2; прочие файлы — 0 изменений; 0 вставок в geometry.ts исключают переименование (мутация F4).**
- [x] 3.2 Перенесённый кейс `lockedDirection` идентичен исходному: фикстура `w40(200, 200, 350, 200)`; `lockedDirection({ x: 340, y: 210 }, { x: 0.085, y: 0.996 }, [a])` → `x` ≈ 0, `y` = 1; `lockedDirection({ x: 340, y: 400 }, { x: 0.085, y: 0.996 }, [a])` → `null`. Ассерты не ослаблены. — **Тело кейса (бывш. :407-415, ныне :285-293) не входило ни в один old_string правок — байт-в-байт; якоря R3 (w40, две группы lockedDirection-ассертов, toBeNull) на месте.**
- [x] 3.3 Живые тесты `geometry.test.ts` вне описанного в 1.3 переноса не изменены; `geometry-cleanup.test.ts` не изменён относительно утверждённого. — **Хунки диффа 1:1 с утверждённым списком (8 хунков); чекер untracked и не менялся с момента валидации.**
