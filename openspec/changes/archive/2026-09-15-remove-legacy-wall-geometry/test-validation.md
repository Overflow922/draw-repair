# Test Validation

Change: remove-legacy-wall-geometry. Дата: 2026-09-15. Свежий контекст, Rule 3: repository и команды только читались/исполнялись; изменён единственный файл — этот отчёт.

**Раунд 2 (повторная валидация).** Раунд 1: VERDICT: FAIL по единственной блокирующей находке F1/R1 (единственный тест живого `lockedDirection` был вложен в удаляемый `describe("snapVertex")` и мог быть потерян всеми зелёными гейтами). Test Writer добавил CLN-COVER-4 в чекер и обновил test-plan/test-suite (R2). Настоящий раунд подтверждает исправления фактическими прогонами и аналитической мутацией.

Фактические прогоны этого раунда:

- `npx vitest run`: 7 файлов, **216 тестов — 214 passed / 2 failed**; красные ровно CLN-GREP-1 и CLN-COVER-4 (оба ожидаемо: red-before-green). CLN-COVER-4 падает на первом ассерте (`geometry-cleanup.test.ts:91` — `describe("lockedDirection"` ещё не создан; его создание и есть шаг реализации).
- `npx tsc --noEmit`: exit 0, чисто; `noUnusedLocals: true` подтверждён в tsconfig.json.
- `git status/diff`: прод (geometry.ts, wall-geometry.ts, main.ts, render.ts) и утверждённые тесты (geometry.test.ts, wall-geometry.test.ts) **не изменены** — Test Writer добавил только `src/geometry-cleanup.test.ts` (новый чекер) и документы change.

## Requirement Coverage

- **PASS**

| Requirement (test-plan) | Test ID | Проверка валидатором |
|---|---|---|
| Удалённые символы нигде не остались | CLN-GREP-1 | Исполнен: красный с точным списком 19 violations (11 в geometry.ts — все 11 имён; 8 в geometry.test.ts). После реализации зелёный = 0 вхождений |
| Компиляция без ссылок на удалённые символы | CLN-TSC-1 | Исполнен: чисто; strict + noUnusedLocals подтверждены |
| Живое поведение сохранено | CLN-VITEST-1 | 214 не-чекерных тестов зелёные; красные — только сами метагейты (ожидаемо) |
| Кейс «клин заливки» покрыт | CLN-COVER-1 | Исполнен, зелёный. HIT-WEDGE-1 (wall-geometry.test.ts:475, прямой клиновой кейс) + HIT-CONS-1 (:498). Зона залива поздней в сцене HIT-CONS-1 (x∈(100,110], y∈(0,10]) накрыта узлами сетки (101..109)×(−12..8), на которых проверяется hitWall === late — проверено чтением геометрии. Формулировка плана уточнена (R2); остаточный косметический недочёт — F6′ |
| Сценарии moveWall сохранены через moveWalls | CLN-COVER-2 | Исполнен, зелёный; эквивалентность 5 сценариев подтверждена чтением (:256-293, группа из одной — :268) |
| 5 блоков snapVertex канонического модуля живы | CLN-COVER-3 | Исполнен, зелёный; имена/строки совпадают с планом (:81, :90, :158, :196, :243) |
| Тест живого lockedDirection переживает удаление describe("snapVertex") | CLN-COVER-4 | Исполнен, красный ожидаемо; три ассерта (контейнер + имя кейса + импорт) проверены против реального файла и мутаций — см. Mutation Testing |

## Boundary Coverage

- **PASS**

- `\bmoveWall\b` не матчит `moveWalls` — подтверждено живым прогоном (в violations один `moveWall` на файл при наличии `moveWalls` в geometry.ts:295) и прямым regex-тестом; также `pointOn` vs `pointOnce`, `snapVertex` vs `snapVertexRadius` — false.
- Скоуп snapVertex подтверждён: wall-geometry.ts, wall-geometry.test.ts, main.ts содержат `snapVertex` и в violations не попали.
- Рекурсия tsFiles: 16 файлов, включая export/font.ts, export/pdf.ts, export/pdf.test.ts; чекер исключает сам себя; SRC_DIR якорится к import.meta.url (независимость от cwd).
- Отказобезопасность: удаление wall-geometry.test.ts / geometry.test.ts роняет CLN-COVER-1/2/3/4 по ENOENT — красный в правильную сторону.
- CLN-COVER-4 не конфликтует с CLN-GREP-1: `lockedDirection` — живое имя, в REMOVED_UNIQUE не входит; переносимый кейс не содержит запрещённых имён.

## Negative Cases

- **PASS**

- «Удалён живой символ» (wallShape, jointTol, lockedDirection, moveWalls, bodyGap): CLN-TSC-1 (внутренние вызовы geometry.ts: wallSegments→wallShape, jointedWalls/attachedEnds/axisAttached/moveWall/moveWalls/snapOthers→jointTol; main.ts:374→lockedDirection; импорты geometry.test.ts) + CLN-VITEST-1. Проверено аналитически по реальным графам вызовов.
- «Частичное удаление» в обе стороны: CLN-TSC-1 (TS2305) + CLN-VITEST-1 (краш загрузки) + CLN-GREP-1 красный. Текущее состояние репозитория и есть такой мутант — гейты красные.
- «Реализация заодно правит оставшиеся тесты» — план объявляет вне автоматизации (дифф-ревью). Раунд 1 требовал закрыть структурную (случайную) потерю кейса lockedDirection — закрыта CLN-COVER-4; умышленное ослабление ассертов остаётся процессным residual (F1-b, принято, см. Mutation Testing).

## Error Handling

- **PASS**

Чекер ошибается только в красную сторону: отсутствующий файл → исключение → красный; violations собираются полным списком и сравниваются `toEqual([])`; ассерты CLN-COVER — жёсткие `toContain`/`toMatch` в цикле, ни одного мягкого.

## Invariants

- **PASS** (раунд 1: FAIL — закрыт)

- «wall-geometry.test.ts не изменяется» — охраняется CLN-COVER-1/3 + исполнением в CLN-VITEST-1. Подтверждено также git diff (файл нетронут).
- «Живые символы и их тесты остаются нетронутыми» — раунд 1 не охранял кейс lockedDirection; теперь CLN-COVER-4 требует и контейнер `describe("lockedDirection"`, и имя кейса «направление от грани фиксируется точно перпендикулярно», и импорт. Инвариант дополнен в test-plan единственным документированным исключением (перенос кейса целиком, без правок ассертов). Мутация F1-a закрыта (см. Surviving Mutations).
- «Поведение приложения, данные, формат хранения — без изменений» — прод не тронут (git diff пуст); клин-кейс и эквивалентность подтверждены.

## State Transitions

- N/A (изменение не создаёт и не меняет состояния; миграций нет).

## Integration Behavior

- **PASS**

Полный `npx vitest run`: 7 файлов / 216 тестов — согласованность geometry.ts / wall-geometry.ts / main.ts / render.ts / export/*. Импорты продовых файлов (main.ts:4-6, render.ts:1-3) не трогают ни один из 11 удаляемых символов; `openspec/specs/` имён не упоминает (grep — 0 файлов). Счётчик 216 в test-suite.md соответствует факту.

## Implementation Independence

- **PASS**

CLN-GREP-1 — текстовый grep, реализацию не воспроизводит. CLN-COVER-1/2/3/4 — presence-гварды над именами тестов; поведение исполняется реальными тестами того же прогона. Двойного воспроизведения геометрии нет. Чекер не требует конкретного способа переноса — только результат (контейнер/имя/импорт).

## Assertion Strength

- **PASS**

- CLN-GREP-1: точное `toEqual([])`, word-boundary regex; контрпримеры (`moveWalls`, `snapVertexRadius`, `pointOnce`) не матчатся — проверено прогоном.
- CLN-COVER-4 проверен против реального файла: A1 (`describe("lockedDirection"`) — false сегодня (корректный red), A2 (имя кейса) — true, A3 (`/\bimport\b[^;]*\blockedDirection\b/`) — true на реальной строке импорта. Тройная избыточность: потеря контейнера ловится A1, потеря кейса — A2, потеря импорта — A3 (+ noUnusedLocals как дубль A3).
- Известные пределы presence-проверок (не понижают вердикт): подделываемость комментариями — F3 (процесс); ослабление ассертов при переносе не ловится — F1-b (принято как residual с R3-якорями дифф-ревью).

## Mutation Testing

- N/A (инструментария мутационного тестирования нет — аналитические мутации на реальном коде и прогонах)

Проверка мутации F1-a «удалил describe("snapVertex") целиком, кейс потерял, импорт вычистил (подсказка noUnusedLocals)» — воспроизведена на синтетическом состоянии файла: A1 остаётся false → CLN-COVER-4 красный; альтернативная ветка «импорт оставлен неиспользуемым» → CLN-TSC-1 красный. Обход через переименование старого describe в `describe("lockedDirection", ...)` со старыми snapVertex-тестами невозможен: тело содержит `\bsnapVertex\b` → CLN-GREP-1 красный. **Мутация F1 раунда 1 убита.**

Мутация F1-b «перенёс кейс, но ослабил ассерты»: A1/A2 остаются истинными, vitest зелёный — гейтами не ловится. Оценка: **приемлемо как residual risk** с контролем через дифф-ревью (R3), поскольку: (1) класс случайной структурной потери — единственный реально опасный для этого изменения — закрыт наглухо; (2) умышленное ослабление ассертов утверждённых тестов — generic-класс, план (Negative Cases) сознательно отдаёт его дифф-ревью, и никакой гейт в принципе не защищает от редактирования самих ассертов; (3) смена стимулов: в раунде 1 CLN-GREP-1 активно толкал реализацию к удалению кейса, теперь ни один гейт не даёт повода его ослаблять — перенос «целиком, без правок ассертов» зафиксирован в Invariants test-plan. Обязательные якоря ревью прописаны в Required Changes R3.

## Surviving Mutations

| Mutation | Expected Failing Test | Result |
|---|---|---|
| Удалён живой символ (wallShape / jointTol / lockedDirection / moveWalls / bodyGap) | CLN-TSC-1 + CLN-VITEST-1 | killed |
| Код удалён, тесты остались (частичное) | CLN-TSC-1 (TS2305) + CLN-VITEST-1 + CLN-GREP-1 | killed |
| Тесты удалены, код остался (частичное) | CLN-GREP-1 | killed |
| describe("snapVertex") удалён целиком вместе с кейсом lockedDirection (мутация F1 раунда 1) | **CLN-COVER-4** (A1; при сохранении неиспользуемого импорта — CLN-TSC-1) | **killed (было SURVIVED)** |
| Кейс перенесён, но ассерты ослаблены (F1-b) | — | SURVIVED — принят как residual, процесс: дифф-ревью по якорям R3 (гейтами не ловится) |
| Переименование старого describe в lockedDirection с сохранением старых snapVertex-тестов (обход COVER-4) | CLN-GREP-1 (вхождения \bsnapVertex\b) | killed |
| Удалён/переименован describe moveWalls или его сценарии | CLN-COVER-2 | killed |
| Удалены/переименованы HIT-WEDGE-1 / HIT-CONS-1 | CLN-COVER-1 | killed |
| Удалён/переименован любой из 5 describe snapVertex (wall-geometry.test.ts) | CLN-COVER-3 | killed |
| Мёртвый код возвращён позже (регрессия) | CLN-GREP-1 | killed |
| Сломано живое поведение при удалении | CLN-VITEST-1 (214 тестов) | killed |
| Мёртвый код переименован вместо удаления (напр. `moveWallLegacy`); релокация в новый файл без переименования тоже ловится — 10 из 11 имён запрещены по всему src/** | — | SURVIVED (процесс: дифф-ревью; якорь: geometry.ts 751→~525 строк) — F4 |
| Удаление экспорта sameTypeJoint «разрешено» правкой живого теста wallShape (:897-898, сняты 2 корреляционных ассерта) | — | SURVIVED (незамечаемо гейтами; потеря мала, правка обязана быть видна в diff) — F2 |
| Ослаблены/вычищены тела прочих живых тестов вне охраняемого списка | — | SURVIVED (процесс: дифф-ревью; план объявляет вне автоматизации) |
| Правка самого чекера (вычёркивание имён / ослабление COVER-4) | — | SURVIVED (процесс: чекер — утверждённый тест, Rule 4 + дифф-ревью; заморожен) |
| Подделка presence-гвардов комментариями (строки COVER в комментариях без реальных тестов) | — | SURVIVED (F3; требует умышленной закладки, видимой в diff; процесс) |

## Findings

- **F1 — ЗАКРЫТО (раунд 2).** CLN-COVER-4 добавлен и проверен: красный сейчас (:red-before-green), убивает мутацию «описал describe целиком — кейс потерян» на ассерте A1, дублирует импорт-контроль через A3 + noUnusedLocals, обход через переименование старого describe блокируется CLN-GREP-1. Остаток F1-b (ослабление ассертов при переносе) — принят как процессный residual с якорями R3.
- **F2 (не блокирующее, для tasks/diff).** `sameTypeJoint` ассертится в живом describe("wallShape") (geometry.test.ts:897-898). Удаление экспорта при нетронутом тесте = CLN-TSC-1 красный (не пройдёт незаметно). Допустимое разрешение — снять ровно эти 2 корреляционных ассерта (основные ожидания формы :885-896 остаются); правка обязана быть видна в diff (включено в R3).
- **F3.** Presence-гварды (COVER-1/2/3/4) подделываемы комментариями; требует умышленной закладки, видимой в diff. Компенсация: охранные тесты исполняются в CLN-VITEST-1, чекер заморожен (Rule 4 + ревью).
- **F4.** Name-based CLN-GREP-1 не ловит переименование мёртвого кода вместо удаления. Якорь ревью: −~226 строк geometry.ts, −~300 строк geometry.test.ts.
- **F5 — ЗАКРЫТО (R2).** Формулировка CLN-COVER-1 в test-plan больше не утверждает, что {109,−9} — узел сетки; корректно указано, что зона накрыта соседними узлами и прямым HIT-WEDGE-1. Незначительный остаточный недочёт F6′: в уточнённой формулировке перечислено y∈{−12,−8,−4} (−4 — не соседний узел к −9) и указан «допуск 6см», тогда как HIT-CONS-1 вызывает hitWall с допуском 0.1 (строже — claim консервативен). На тесты и реализацию не влияет.
- **F6′ (документ, не блокирующее).** В test-suite.md строка CLN-GREP-1 таблицы всё ещё содержит устаревшее «17 вхождений в geometry.ts»; корректный итог — в примечании под таблицей: 19 = 11 + 8. Рекомендуется поправить при архивации, чтобы не тиражировать опечатку.
- **F7.** Эквивалентность moveWall→moveWalls и 5 блоков snapVertex подтверждена чтением, не только строками чекера. Эквивалентность старых snapVertex-сценариев каноническим — на уровне классов поведения (семантика hCm-блоков заменена на радиус+край квадрата); сведение принято на этапе планирования.
- **F8 (положительный).** Тест-райтер не тронул ни прод, ни утверждённые тесты (git diff пуст) — правило «чикер и документы, ничего больше» соблюдено; CLN-COVER-4 не создаёт конфликтов с CLN-GREP-1 и не нарушает Rule 4 (новый тест данного change).

## Required Changes

- Изменений в тестах не требуется (R1 выполнен и верифицирован).
- **R3 (этап Apply — включить в tasks.md, процесс).** Чеклист дифф-ревью реализации:
  1. Кейс lockedDirection перенесён в `describe("lockedDirection")` **целиком, без правок ассертов**; якоря тела кейса: `lockedDirection({ x: 340, y: 210 }, { x: 0.085, y: 0.996 }, [a])` → `toBeCloseTo(0)` и `.toBe(1)`; `lockedDirection({ x: 340, y: 400 }, …)` → `toBeNull()`; фикстура `w40(200, 200, 350, 200)`. Ассертов в переносимом кейсе — ровно три группы, как в исходнике (:407-415).
  2. Удалены только перечисленные в proposal тест-блоки; правка :897-898 (sameTypeJoint в живом describe wallShape) ограничена снятием двух корреляционных ассертов и видна в diff.
  3. `src/geometry-cleanup.test.ts` не изменён реализацией (заморожен).
  4. Якорь объёма: geometry.ts 751→~525 строк, geometry.test.ts 939→~640 строк; wall-geometry.ts и wall-geometry.test.ts — без изменений.
  5. Повторный греп перед удалением каждого символа (D2): если символ оказался живым — исключить из удаления и зафиксировать в отчёте задачи.
- Косметика документам (по желанию, на этап Test Writing/архивации): F6′ (устаревшее «17» в строке таблицы test-suite.md) и F6′-соседняя неточность «−4 / допуск 6см» в test-plan (факт: соседние узлы −12/−8, допуск HIT-CONS-1 = 0.1).

Production code, тесты, fixtures, specification, design, test-plan валидатором не изменялись (Rule 3).

## Verdict

VERDICT: PASS

Основание раунда 2: блокирующая находка F1 закрыта CLN-COVER-4, проверенным фактическим прогоном (красный сегодня, ожидаемо) и аналитической мутацией (удаление describe вместе с кейсом — гарантированно красный; обходные пути блокированы CLN-GREP-1/CLN-TSC-1). Все требования test-plan покрыты исполняемыми проверками; границы, негативные случаи, инварианты, сила ассертов и независимость от реализации подтверждены; остаточные риски (F1-b, F2, F3, F4) — процессного класса, явно зафиксированы и закрыты чеклистом R3. Прогоны: 214 passed / 2 failed (ровно ожидаемо-красные CLN-GREP-1 и CLN-COVER-4), tsc чист. После реализации все 216 тестов должны стать зелёными — это и есть CLN-VITEST-1.
