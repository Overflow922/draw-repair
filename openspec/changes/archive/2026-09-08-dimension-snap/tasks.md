## 1. Геометрия снапа

- [x] 1.1 В geometry.ts добавить `dimLevelSnap(p, axis, dimensions, walls, radius)`: для размеров с осью, параллельной оси черновика (|cross| ≈ 0), уровень = `dimensionOffsetAt(p1 соседа, axis)`; при |offset курсора − уровень| ≤ radius вернуть снапнутый offset и точку (проекция курсора на линию уровня), иначе null; сосед с `dimPointPoint` = null пропускается. Тесты в geometry.test.ts: параллельный сосед в радиусе, вне радиуса, антипараллельная ось, непараллельная ось, два соседа на разных уровнях (берётся ближайший), битая ссылка соседа — `npm test`

## 2. Привязка при постановке размера

- [x] 2.1 В main.ts на шагах точек замера вычислять кандидата `nearestEdgeIntersection(cursor, …)` в redraw, передавать точку в render как опцию снапа; резинку второго шага заканчивать в точке-кандидате, а не в сеточном курсоре — проверка: `npx tsc --noEmit`
- [x] 2.2 В render.ts отрисовать отметку снапа: залитый круг ~3.5 px (экранные координаты), красный с белой обводкой; опция без отметки — рисования нет — проверка: `npx tsc --noEmit`
- [x] 2.3 В placeDimension третий клик: offset = результат `dimLevelSnap`, иначе прежнее `dimensionOffsetAt(p, axis)`; превью третьего шага (dimDraftGeometry) использует тот же снапнутый offset — превью совпадает с коммитом — проверка: `npm test`, браузерно: цепочка из двух размеров выравнивается по уровню

## 3. Привязка при перетаскивании

- [x] 3.1 В ветке dimDrag (pointermove) применить `dimLevelSnap` с исключением перетаскиваемого размера по ссылке; вне радиуса — прежнее поведение; история жеста — один шаг — проверка: `npm test`, браузерно: перетаскивание линии к соседу прилипает, одно «Отменить»

## 4. Верификация

- [x] 4.1 Полный прогон: `npm test`, `npm run build`, `openspec validate dimension-snap`; браузерная проверка всех сценариев спеки (отметка точки, резинка от кандидата, снап третьего клика, снап drag) — синтетическими pointer-событиями, как в dimension-selection
