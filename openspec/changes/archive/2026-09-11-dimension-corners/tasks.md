## 1. Геометрия: примитив контурных точек

- [x] 1.1 В geometry.ts добавить функцию отрезков контура (стороны `wallShape` ↔ edge 0/1/2/3) и резолв пары граней через пересечение отрезков с касанием концом; вырожденные случаи (параллельность, коллинеарное перекрытие) → fallback; fallback = ближайший к чужому отрезку конец одного из отрезков. Тесты: угол одной стены, митра (непрямой угол одного типа), примыкание/T-стык (касание концом), три стены в вершине, разошедшийся стык (fallback), коллинеарные грани (fallback)
- [x] 1.2 Заменить резолв точки замера (`dimPointPoint`-вызовы в render/main: `endPoint`, `dimHitDistance`, отрисовка, значение) на новый примитив; тесты значений обновить под вершины контура в стыках (свободные углы — без изменений)
- [x] 1.3 Перевести снап постановки (`nearestEdgeIntersection`) на пары отрезков контура с приоритетами (своя стена → конец-конец → касание концом); тесты: фантомы (пересечение продолжений за торцом, осевая точка торца) не находятся, вершина стыка находится, приоритет пары своей стены, вне радиуса — null
- [x] 1.4
- [x] 1.5 Fallback `segClamp` → ближайшая точка первого отрезка ко второму (closest-point пары отрезков) вместо примерзания к концевой точке: legacy-пары, потерявшие касание, скользят по грани за геометрией. Тесты: три fallback-сценария сохраняют ожидания (параллельные/коллинеарные вырождаются в концевую точку) Проверить производительность: `wallShape` строится один раз на стену за проход снапа/резолва (npm run test + ручный прогон перетаскивания при большом числе стен)

## 2. Интеграция в main.ts

- [x] 2.1 `placeDimension` и снап-hit превью (`redraw`) используют новый снап; отметка снапа и резинка заканчиваются в вершине контура (тесты существующих сценариев постановки зелёные)
- [x] 2.2 Поведение следования: поворот/растяжение конца и сдвиг соседа по стыку — точка замера остаётся на вершине контура (тест: угол стыка 90°→120°→параллельность не даёт null и перескока), расхождение стыка — прилипание к вершине
- [x] 2.3 `dimLevelSnap`, перетаскивание размерной линии, undo/redo, хранилище — без изменений (существующие тесты зелёные без правок этих модулей)

## 3. Верификация

- [x] 3.1 Полный прогон: `npm run test`, `npx tsc --noEmit`, `npm run build`, `openspec validate --all` — зелёные
- [x] 3.2 Браузерная проверка синтетическими pointer-событиями: постановка по вершинам (фантомов нет), поворот стены с размером — размер следует за углами контура, удаление соседа — размер прилип к вершине; дев-сервер оставить запущенным, ссылку в отчёт
