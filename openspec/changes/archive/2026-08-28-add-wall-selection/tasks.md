## 1. Геометрия попаданий

- [x] 1.1 Добавить `hitWall(p, walls, toleranceCm)` в `src/geometry.ts` (расстояние до осевой ≤ max(толщина/2, допуск), ближайшая при перекрытии) и юнит-тесты в `src/geometry.test.ts`: попадание внутри полосы тонкой стены только в пределах допуска, попадание в толстую стену вне допуска но в полосе, приоритет ближайшей из двух. Проверка: `npm test`
- [x] 1.2 Добавить `endpointAt(p, wall, radiusCm)` в `src/geometry.ts` (возврат `"a" | "b" | null`) и юнит-тесты: попадание в каждый конец, промах в середину стены. Проверка: `npm test`

## 2. Отображение выделения

- [x] 2.1 Расширить `render` в `src/render.ts` параметром выделенной стены: обводка контрастным полупрозрачным штрихом под стеной и маркеры концов постоянного экранного размера (радиус `HANDLE_PX / k`, белая заливка, тёмная обводка). Проверка: `npm run dev` — временно передать стену в выделение и убедиться, что обводка видна на всех трёх типах стен и маркеры не меняют размер при зуме

## 3. Интеракция

- [x] 3.1 В `src/main.ts` добавить состояние `selectedWall` и ветвление в обработчике `click`: вне цепочки клик по стене (`hitWall` с допуском `SNAP_RADIUS_PX / k`) выделяет её без начала цепочки, клик по пустому месту снимает выделение и начинает цепочку как раньше; Esc при отсутствии цепочки снимает выделение. Проверка: `npm run dev` — сценарии «Выделение кликом», «Перенос выделения», «Клик по пустому месту», «Esc снимает выделение», «Клик во время цепочки не выделяет» из specs/wall-selection
- [x] 3.2 Реализовать перетаскивание конца: pointerdown левой кнопкой на маркере конца выделенной стены (`endpointAt`) начинает drag с `setPointerCapture`; на move — `snap` с `orthoFrom` = противоположный конец и исключением перетаскиваемой стены из кандидатов, точка пишется в конец стены и `redraw()`; совпадение с противоположным концом (`pointsEqual`) пропускается; флаг `suppressClick` гасит следующий `click`. Проверка: `npm run dev` — сценарии «Перетаскивание конца меняет длину», «Привязка к соседней стене», «Подпись длины обновляется», «Нулевая длина не применяется»; pan средней кнопкой и зум работают во время выделения
- [x] 3.3 Связать свойства с выделенной стеной: `syncThicknessBox` берёт толщину из `selectedWall?.thicknessCm ?? thicknessCm`; `input` толщины при выделении пишет валидное значение в стену и в дефолт; `setWallType` при выделении меняет `wall.type` (и активный образец) — включая стрелки вверх/вниз; при выделении вызвать `setWallType(wall.type)` и обновить поле толщины; после снятия выделения значения не сбрасывать. Проверка: `npm run dev` — сценарии «Толщина выделенной стены меняется», «Смена типа перекрашивает выделенную стену», «Смена типа не перекрашивает невыделенные стены», «Поле толщины отражает выделенную стену», «Значения остаются после снятия выделения»; конвертация поля при смене единицы работает при выделении

## 4. Проверка

- [x] 4.1 Прогнать `npm test` и `npm run build` (tsc + vite) без ошибок
- [x] 4.2 Пройтись вручную по всем сценариям specs/wall-selection/spec.md и модифицированным требованиям specs/wall-drawing и specs/wall-types; убедиться, что существующее поведение (рисование цепочки, точная длина, единицы, pan/зум) не регрессировало

## 5. Стыки и клавиатурный ресайз

- [x] 5.1 Добавить `moveEndpoint(walls, wall, end, pos)` в `src/geometry.ts` (перемещает конец и все совпадающие с ним концы других стен) и юнит-тесты: перемещение конца, протяжка совпавшего конца соседа, нетронутый несовпадающий сосед. Проверка: `npm test`
- [x] 5.2 Использовать `moveEndpoint` в drag конца (`src/main.ts`): соседние стены следуют за стыком. Проверка: `npm run dev` — сценарий «Соседняя стена следует за стыком» из specs/wall-selection
- [x] 5.3 Клавиатурное изменение длины выделенной стены: живое значение длины в поле (`liveLengthCm`), ввод немедленно меняет длину (a неподвижен, b вдоль направления, через `moveEndpoint`), сброс `lengthDirty` при выделении и снятии выделения. Проверка: `npm run dev` — сценарии «Поле показывает длину выделенной стены», «Ввод меняет длину», «Соседний стык следует при вводе длины», «Некорректное значение», «Поле очищается при снятии выделения»
- [x] 5.4 Прогнать `npm test`, `npm run build`, `openspec validate add-wall-selection`

## 6. Привязка за концом стены (угол >90°)

- [x] 6.1 `snap` в `src/geometry.ts`: кандидат — продолжение линии стены за пределом отрезка, когда конец вне радиуса привязки (конец в радиусе приоритетен); юнит-тесты: протяжка к продолжению линии, приоритет конца. Проверка: `npm test`
- [x] 6.2 Проверка в браузере: перетаскивание конца выделенной стены к продолжению линии другой стены (за её концом) — конец привязывается к линии; регрессия существующих привязок рисования. Проверка: `npm run dev`, `npm run build`, `openspec validate add-wall-selection`

## 7. Переключатель орто-привязки

- [x] 7.1 Кнопка `#ortho-toggle` в `index.html` (классы `.tool`/`.active`) и флаг `ortho` в `src/main.ts`: условная передача `orthoFrom` в `snap` при рисовании (`toSnappedPoint`) и при drag конца. Проверка: `npm run build`
- [x] 7.2 Проверка в браузере: по умолчанию включена (орто работает при рисовании и drag); после выключения сегмент и перетаскиваемый конец не притягиваются к осям, привязка к стенам и сетке сохраняется; повторное включение возвращает орто. Проверка: `npm run dev`, `openspec validate add-wall-selection`

## 8. Перенос переключателя орто в блок размеров

- [x] 8.1 Переместить `#ortho-toggle` из `#toolbar` в `#size-box` (`index.html`), стилизовать `#ortho-toggle` в `src/style.css` в габаритах кнопок единиц. Проверка: `npm run build`, `npm run dev` — кнопка в блоке размеров, переключение работает, визуальное состояние отображается
- [x] 8.2 Орто-привязка выключена по умолчанию: `ortho = false` в `src/main.ts`, без класса `active` у `#ortho-toggle` (`index.html`), сценарий «Состояние по умолчанию» в дельте specs/wall-drawing обновлён. Проверка: `npm run build`, `npm run dev` — после загрузки кнопка не подсвечена, сегмент в пределах 15° от оси не притягивается
