## 1. Переключатель единиц

- [x] 1.1 Добавить в панель сегмент м/см/мм с визуально выделенной активной единицей (`index.html`, `src/style.css`, состояние и обработчик в `src/main.ts`); проверить в браузере переключение и подсветку кнопки

## 2. Подписи длины

- [x] 2.1 Реализовать в `src/render.ts` `formatLength` и отрисовку подписи вдоль стены с белой обводкой для нарисованных стен и превью; проверить в браузере центрирование на середине стены и смену ширины подписи при переключении единиц («1,5 м» → «150 см» → «3000 мм»)

## 3. Итоговая проверка

- [x] 3.1 Прогнать все сценарии из `specs/wall-dimensions/spec.md` в dev-сервере; убедиться, что `npx vitest run` и `npm run build` проходят без ошибок
