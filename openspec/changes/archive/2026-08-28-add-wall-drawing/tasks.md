## 1. Каркас приложения

- [x] 1.1 Создать scaffold Vite vanilla-ts в корне проекта (`npm create vite@latest . -- --template vanilla-ts`), выполнить `npm install`; проверить, что `npm run dev` открывает страницу в браузере
- [x] 1.2 Свёрстать раскладку: вертикальная панель инструментов слева фиксированной ширины + `<canvas>`, занимающий остальную область окна (`index.html`, CSS); проверить визуально: панель слева, холст заполняет остальное при ресайзе окна
- [x] 1.3 Настроить canvas: масштабирование backing store на `devicePixelRatio`, константы `PX_PER_CM = 2` и шаг сетки 10 см; отрисовать сетку; проверить чёткость линий сетки на hi-dpi-экране

## 2. Геометрия привязок

- [x] 2.1 Создать `src/types.ts` (`Point`, `Wall`) и `src/geometry.ts`: чистая функция привязки `snap(cursor, walls, gridStepCm, radiusCm)` с приоритетом «конец стены → проекция на сегмент → узел сетки» и выбором ближайшего кандидата; добавить фильтр сегментов нулевой длины
- [x] 2.2 Подключить Vitest (`npm install -D vitest`); покрыть тестами сценарии спеки: привязка к концу стены, T-образный стык, приоритет стен над сеткой, клик рядом с узлом сетки; проверить, что `npx vitest run` проходит

## 3. Инструмент «Стена»

- [x] 3.1 Добавить в панель кнопку инструмента «Стена» с визуальным выделением активного состояния и поле ввода толщины в см с валидацией (некорректные значения не применяются, остаётся последнее валидное); проверить вручную сценарии «Задание толщины» и «Некорректная толщина»
- [x] 3.2 Реализовать логику цепочки в `src/main.ts`: клик фиксирует вершину через `snap`, следующий сегмент начинается из последней вершины, превью-сегмент следует за курсором, Esc и двойной клик завершают цепочку, дубликаты вершин от двойного клика отбрасываются фильтром нулевой длины; проверить вручную сценарии «Первый сегмент», «Предварительный сегмент», «Завершение цепочки»
- [x] 3.3 Реализовать отрисовку стен в `src/render.ts`: `stroke` сегмента с `lineWidth = thicknessCm * PX_PER_CM` и `lineCap = "square"`, полупрозрачный превью; толщина хранится в каждой стене при создании; проверить, что смена толщины не меняет нарисованные стены

## 4. Итоговая проверка

- [x] 4.1 Прогнать вручную все сценарии из `specs/canvas-app/spec.md` и `specs/wall-drawing/spec.md` в dev-сервере; убедиться, что `npm run build` проходит без ошибок; при расхождении с `design.bmp` подогнать вид панели (иконки/отступы)
