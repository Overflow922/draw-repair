## 1. Состояние инструментов

- [x] 1.1 Ввести `tool: "wall" | "eraser" | "none"` и централизованный `setTool` (гашение `chainStart`, сброс `selectedWall`, закрытие `wallPanel`, синхронизация `.active` кнопок и класса курсора на канвасе); стартовое состояние — `wall`; сброс в `wall` в `activate()`. Проверить в браузере: кнопки переключают активность, после смены вкладки — снова «Стена».
- [x] 1.2 Добавить кнопку «Ластик» в `#toolbar` (`index.html`) и стили курсора для режима ластика (`style.css`). Проверить: кнопка отображается рядом с «Стеной», класс курсора применяется.

## 2. Семантика кликов

- [x] 2.1 Развести обработчик `click` по состояниям: `wall` — цепочка/выделение (текущее поведение), `eraser` — удаление по `hitWall`, клик по пустому месту ничего; `none` — только выделение. Проверить в браузере все 6 ячеек таблицы кликов из design.md D1.

## 3. Удаление

- [x] 3.1 Общая функция удаления: `record(drawingHistory(...), walls)` → `splice` → сброс `selectedWall` при удалении выделенной → `dirty` → `redraw()`; вызывается из клика ластиком. Проверить в браузере: серия удалений подряд, undo возвращает стену с толщиной и типом, redo удаляет снова.
- [x] 3.2 Обработчик Delete в глобальном `keydown` с guard `e.target instanceof HTMLInputElement`, только `e.key === "Delete"`, работает при любом `tool` при наличии выделения. Проверить в браузере: Delete удаляет выделенную стену; при фокусе в поле длины, толщины и в переименовании вкладки — не удаляет.

## 4. Подсветка ластика

- [x] 4.1 Hover-подсветка: параметр подсвечиваемой стены в `render()` (красный контур тем же кодом, что обводка выделения), значение из `hitWall(cursor, ...)` в `pointermove` при `tool === "eraser"`. Проверить в браузере: стена под курсором краснеет, подсветка исчезает при уходе курсора и после Escape, подсветка всегда совпадает с тем, что удалит клик.
- [x] 4.2 Escape при активном ластике: `setTool("none")`, подсветка и курсор ластика исчезают, «Стена» не включается. Проверить в браузере.

## 5. Верификация

- [x] 5.1 `npm test` (vitest), `npm run build` (tsc + vite) — зелёные; существующие тесты истории/геометрии не тронуты. Прогнать все сценарии спек `wall-deletion` и `wall-selection` по браузерному чек-листу.
