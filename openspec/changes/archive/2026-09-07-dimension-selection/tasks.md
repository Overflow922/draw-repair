## 1. Состояние выделения и разметка панели

- [x] 1.1 `index.html`: обернуть кнопку «Размер» в `.tool-anchor`, добавить панель `#dim-panel` с полем «Вынос» и read-only «Значение»; стили в `style.css` по образцу `#wall-panel`. Проверка: `npx tsc --noEmit` и `npm run build` зелёные.
- [x] 1.2 `main.ts`: `selectedDimension`, хелперы выделения/сброса, взаимное исключение с `selectedWall` и панелями (выделение размера закрывает панель стены, и наоборот). Проверка: `npx tsc --noEmit`.

## 2. Интеракции выделения

- [x] 2.1 Нажатие по размерной линии/числу (не ластик) выделяет размер и продолжает существующий `dimDrag`; клик без движения оставляет выделенным; клик по стене снимает выделение размера; клик по пустому месту снимает выделение (цепочка — только в инструменте «Стена»). Проверка: браузером — клик/drag по размеру в инструментах «Стена», «Размер», «Без инструмента».
- [x] 2.2 Сбросы: порядок Esc (цепочка → черновик → выделение размера → выключение инструмента → выделение стены), `setTool`, `activate` (вкладка), `resetEditing` (undo/redo), каскад в `deleteWall` (выделенный размер отфильтрован → сброс + закрытие панели). Проверка: браузером все ветки сброса.

## 3. Панель свойств

- [x] 3.1 Поле «Вынос»: ввод применяет offset немедленно (текущая единица, запятая/точка, 0 и отрицательные допустимы, нечисловое игнорируется); live-обновление поля при drag; «Значение» — read-only расстояние между точками замера; открытие при выделении, закрытие при снятии. Проверка: браузером ввод «400», «-300», мусор, drag.

## 4. Отрисовка выделения

- [x] 4.1 `render.ts`: выделенный размер — акцентная подсветка линии, стрелок, выносных и числа; маркеры на концах размерной линии (p1/p2), без маркеров на точках замера; после снятия выделения обычный вид. Проверка: `npx tsc --noEmit`, браузером вид выделенного/обычного размера.

## 5. Верификация

- [x] 5.1 Прогнать `npm test`, `npx tsc --noEmit`, `npm run build`; `openspec validate --all`. Проверка: всё зелёное.
- [x] 5.2 Браузерная верификация сценариев спеки `dimension-selection` (выделение во всех инструментах, взаимное исключение, панель, Delete, Esc-порядок, каскад удаления стены, undo/redo, вкладки). Dev-сервер оставить работать для проверки пользователем; остановить только после архивации.
