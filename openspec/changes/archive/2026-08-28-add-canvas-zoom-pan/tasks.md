## 1. Чистая математика вида

- [x] 1.1 Добавить в `types.ts` константы `ZOOM_MIN = 0.1`, `ZOOM_MAX = 10`; в `geometry.ts` — функцию пересчёта вида при зуме к точке (кламп zoom, `pan' = worldUnderCursor − screen / k'`) и функцию видимого мирового прямоугольника; покрыть обе тестами в `geometry.test.ts`, проверить `npm test`

## 2. Рендер через вид

- [x] 2.1 Передать вид (`zoom`, `pan`) в `render()` из `main.ts`; стены и сетку рисовать через `translate`/`scale(k, k)`, сетку — по видимому мировому прямоугольнику из задачи 1.1 с `lineWidth = 1/k`; подписи длин рисовать в экранных координатах фиксированным кеглем; проверить `npm run build` и визуально в `npm run dev` (начальный вид 100% неотличим от текущего)

## 3. Ввод

- [x] 3.1 Добавить на канву `wheel`-обработчик с `{ passive: false }`: зум к курсору функцией из 1.1 + `redraw()`; проверить, что точка чертежа под курсором неподвижна при зуме и страница не скроллится
- [x] 3.2 Добавить пан: `pointerdown` с `e.button === 1` — `preventDefault()` + `setPointerCapture`, сдвиг `pan` от абсолютной позиции указателя; во время пана `cursor`/превью не обновляются, после отпускания — пересчёт; подавить `auxclick`; проверить: пан не меняет зум, средний клик без движения не создаёт и не завершает цепочку, автоскролл браузера не появляется
- [x] 3.3 Перевести `toSnappedPoint` на экран→мир через вид, радиус привязки к стенам — `SNAP_RADIUS_PX / k`; проверить в `npm run dev`: привязка к концам/T-стыкам при увеличенном и уменьшенном виде, орто-привязка, точная длина не зависит от масштаба

## 4. Итоговая проверка

- [x] 4.1 Прогнать `npm test` и `npm run build`; вручную проверить все сценарии спеки `canvas-viewport` (зум к курсору, пределы, пан, сетка в мировых координатах, начальный вид, рисование не затронуто)
