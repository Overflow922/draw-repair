## 1. Геометрия прилипания (geometry.ts)

- [x] 1.1 Удалить `jointPullback` и его использование в `commitPoint`/`redraw`; `npx vitest run` проходит (тестов на pullback нет, компиляция чистая)
- [x] 1.2 Удалить `healJoints` (функция, вызов в main.ts:54, тесты в geometry.test.ts); `npx vitest run` проходит
- [x] 1.3 Реализовать контактный snap квадрата: кандидаты (2 грани + 2 торца каждой стены), типы контакта «грань»/«торец», позиции оси по D1, правило выбора D2 (проекция за торцом → грань, в полосе → торец), приоритет стен над сеткой; юнит-тесты: T-контакт, угловой контакт, коллинеарное продолжение, выбор за торцом/в полосе, приоритет над сеткой, вне радиуса → сетка
- [x] 1.4 Юнит-тест инварианта: оси стен после прилипания не совпадают и отстоят больше допуска стыка (`jointAt` для таких пар возвращает null → `wallShape` обеих стен — чистые прямоугольники)

## 2. Легаси-рендер прямых углов (geometry.ts, render.ts)

- [x] 2.1 В `endCap` вернуть немитровую угловую ветку поздней стены к доведению торца до дальней грани ранней; тест: совпадающие оси 90° одного типа — ранняя плоская, поздняя доведена до дальней грани, наружный угол закрыт без выемки
- [x] 2.2 Убрать в `sameTypeJoint` исключение прямого угла; обновить тесты «одинаковые стены под 90°: торцы рисуются» на слияние (true) — geometry.test.ts:364-369 и 773-774
- [x] 2.3 Подрезка рёбер контура внутри тела соседа в `strokeContour` (полуплоскость границы соседа в вершине стыка); тест на геометрию подрезки: у рёбер нет участков внутри тела соседней стены

## 3. Заливка клина (geometry.ts)

- [x] 3.1 Классификация стыка: прямой (допуск RIGHT_ANGLE_MAX_COS) / коллинеарный / непрямой; удалить мёртвую логику MITER_MIN; юнит-тесты классификации на 90°, 45°, 20°, 175°, 180°
- [x] 3.2 Построение клина (lineIntersect-примитивы) для углового и T-примыкания на обоих концах; тесты: 45° угловой, 60° T, клин ограничен телом ранней стены
- [x] 3.3 `wallFillShape` = прямоугольник ∪ клинья − тела более ранних соседей (полуплоскостное отсечение); тесты: клин принадлежит полигону поздней стены, части поздней внутри ранней отсечены, у чисто уложенных стен fillShape == прямоугольник
- [x] 3.4 Point-in-polygon fallback в hitWall/eraserTarget (D9); тест: точка в клине попадает в позднюю стену

## 4. Интеграция в рисование (main.ts)

- [x] 4.1 Контактный snap в `toSnappedPoint` для инструмента «Стена» (оба конца цепочки, повторное прилипание старта сегмента при повороте — D3); проверка в браузере: рисование комнаты-прямоугольника даёт 4 замкнутых угла без выемок и наложений
- [x] 4.2 Квадрат-превью в `render` (до первого клика за курсором, на свободном конце сегмента, сторона = thicknessCm; D8); проверка: сторона квадрата меняется с толщиной, в других инструментах не рисуется
- [x] 4.3 Приоритет точной длины над прилипанием второго конца сохранён (lengthDirty); проверка: с введённой длиной конец по направлению и длине, тело начинается от прилипшего старта
- [x] 4.4 Отмена авто-продолжения цепочки (запрос пользователя): стена ставится одним жестом из двух кликов, после фиксации инструмент ждёт новый старт; дельта-спека `wall-drawing` «Рисование цепочки стен» переписана на жест из двух кликов; проверка в браузере: после фиксации стены от её конца не рисуется превью, только квадрат

## 5. Отрисовка (render.ts)

- [x] 5.1 `drawMaterial`/`strokeContour`/`drawOutline` перевести на `wallFillShape`; wallShape остаётся для `wallSegments`/размеров; проверка в браузере: штриховка клина материалом новой стены, у одного типа шов не рисуется, у разных — прямой шов
- [x] 5.2 Проверка непрерывности штриховки через прямой стык и клин (фаза общая, двойной штриховки нет) — визуальная проверка в браузере + PDF-экспорт страницы со стыками

## 6. Спецификации и верификация

- [x] 6.1 Прогнать `npx vitest run` — весь набор зелёный; `npm run build` — компиляция чистая
- [x] 6.2 Браузерная проверка сценариев спеки: комната-прямоугольник, T-примыкание 90°, 45° и 60° (заливка клина), коллинеарное продолжение, удаление стены с клином (ранняя остаётся), установка стены не меняет выделенную
- [x] 6.3 Легаси-проверка: открыть чертёж со старыми совпадающими осями — углы закрыты, оси в файле не изменились (сохранить/перечитать JSON)
- [x] 6.4 `openspec validate flush-wall-placement` — валидация изменений
