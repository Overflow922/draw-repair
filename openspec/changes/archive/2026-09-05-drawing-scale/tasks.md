## 1. Модель и хранение

- [x] 1.1 В `src/types.ts`: `SCALE_DENOMINATORS = [50, 100, 200, 500]`, поле `scale` в `Drawing`; тест `types.test.ts` на ряд и дефолт
- [x] 1.2 В `src/storage.ts`: версия 2, параметризованный валидатор, миграция v1 → `scale: 100`; тесты: v1-фикстура открывается с 1:100, первое сохранение пишет v2, битый `scale` в v2 — повреждённые данные (`npm test`)

## 2. Размещение и доступность форматов

- [x] 2.1 В `src/export/pdf.ts`: размещение по `mmPerCm = 10 / X` (центрирование, автоориентация, поля), удаление `fitToPage`; тест: стена 5 м при 1:100 = ровно 50 мм на листе, смещения центрируют bbox в полях
- [x] 2.2 В `src/export/pdf.ts`: `availableFormats(walls, scale)` в порядке A4→A0; тесты: вмещающие/невмещающие форматы для разных bbox и масштабов, пустой список при переполнении A0
- [x] 2.3 Обновить существующие тесты `pdf.test.ts` под новый контракт размещения и имя файла (`npm test`)

## 3. UI

- [x] 3.1 В `index.html`/`src/main.ts`: селектор `#pdf-scale` в `#size-box`; смена пишет масштаб активного чертежа, синхронизация при переключении вкладок и создании чертежа; зум и подписи не меняются
- [x] 3.2 Фильтрация `#pdf-format` из `availableFormats`: сохранение выбора либо наименьший доступный; пустой список — select и экспорт неактивны
- [x] 3.3 Попап-остров «Невозможно экспортировать в <форматы> — чертёж не влезает»: показ при расширении набора невмещающих форматов, без показа на первом рендере и при сужении

## 4. Верификация

- [x] 4.1 `npm test`, `npm run build` — зелёные
- [x] 4.2 Браузерная проверка: рисование при 1:100, смена масштабов, исчезновение форматов и попап, экспорт A4/A3 с точным соотношением (5 м → 50 мм при 1:100), перезагрузка восстанавливает масштабы, старое хранилище v1 мигрирует
