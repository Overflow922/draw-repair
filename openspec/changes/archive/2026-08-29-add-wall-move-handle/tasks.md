## 1. Геометрия

- [x] 1.1 Добавить в `src/geometry.ts` `handleAt(p, wall, radiusCm): "a" | "b" | "mid" | null` (концы проверяются первыми, затем середина `(a+b)/2`); покрыть тестами в `src/geometry.test.ts`: попадание в середину, приоритет конца на короткой стене, промах вне радиуса. Проверка: `npm test`.
- [x] 1.2 Добавить в `src/geometry.ts` `moveWall(walls, wall, delta: Point)`: сдвиг `a`/`b` на вектор и протяжка концов других стен, совпадающих со старыми позициями (`pointsEqual`); тесты: оба конца сместились, приваренные концы соседей следуют, несвязанные стены не изменились. Проверка: `npm test`.

## 2. Интеракция

- [x] 2.1 В `src/main.ts`: состояние `wallMove` (снимок `baseA`/`baseB`, точка захвата `grab`), захват среднего маркера на pointerdown выделенной стены через `handleAt` (после проверки концов), `setPointerCapture`, `suppressClick`. Проверка: клик по середине без движения не снимает выделение и не начинает цепочку (вручную).
- [x] 2.2 Обработка pointermove при `wallMove`: `rawDelta`, кандидаты привязки — все стены кроме перемещаемой и состыкованных с ней, `snap(baseA + rawDelta, …, ortho ? baseA : undefined)`, применение `moveWall` с итоговой дельтой; сброс на pointerup; длина/подпись/поле длины не меняются. Проверка: перетаскивание середины двигает стену целиком (вручную), привязка к стенам/сетке/орто работает (вручную), состыкованная стена не прилипает к стыку (вручную).

## 3. Отрисовка

- [x] 3.1 В `src/render.ts` расширить `drawHandles` третьим маркером в середине стены (тот же стиль); маркеры исчезают при снятии выделения. Проверка: `npm run build` проходит, визуально на трёх типах стен.

## 4. Верификация

- [x] 4.1 Прогнать полный набор сценариев спеки `wall-selection` по перемещению за средний маркер вручную: перемещение целиком, следование соседей, привязка к несостыкованной стене, стык не блокирует, орто вкл/выкл, клик по маркеру, неизменность длины. Проверка: чек-лист пройден, `npm test` и `npm run build` без ошибок.
