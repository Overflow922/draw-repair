## 1. Орто-привязка 90°

- [x] 1.1 Расширить `snap` параметром `orthoFrom`: притяжение к горизонтали/вертикали при отклонении ≤15°, приоритет стен сохранён, свободная координата округляется к сетке (`src/geometry.ts`); проверить 5 юнит-тестов орто-привязки
- [x] 1.2 Передавать последнюю вершину цепочки как `orthoFrom` в `toSnappedPoint` (`src/main.ts`); проверить в браузере строго горизонтальное/вертикальное превью и коммит, свободную диагональ и комбинацию с точной длиной

## 2. Типы стен

- [x] 2.1 Добавить `WALL_TYPES`/`WallType` и `Wall.type` (`src/types.ts`), цвет стен по типу в рендере (`src/render.ts`), состояние и селектор цветными образцами в панели с дефолтом «Перегородка» (`src/main.ts`, `index.html`, `src/style.css`); проверить в браузере цвета превью и коммита для трёх типов и что смена типа не перекрашивает нарисованные стены
- [x] 2.2 Переключение типа стрелками ↑/↓ с зацикливанием через общий `setWallType`, стрелки в поле толщины не перехватываются; проверить в браузере порядок переключения, зацикливание, смену цвета превью в середине цепочки и неприкосновенность степпера толщины

## 3. Итоговая проверка

- [x] 3.1 Прогнать все сценарии из `specs/wall-drawing/spec.md` и `specs/wall-types/spec.md` в dev-сервере; убедиться, что `npx vitest run` (12 тестов) и `npm run build` проходят без ошибок
