## 1. Блок размеров

- [x] 1.1 Перенести переключатель м/см/мм из панели инструментов в блок `#size-box` в правом нижнем углу холста вместе с полем длины (`index.html`, `src/style.css`); проверить позицию блока (отступы 12 px от краёв) и крупнее шрифт единиц
- [x] 1.2 Увеличить подписи длин на холсте до 14 px с обводкой 4 px (`src/render.ts`); проверить высоту текста подписи в браузере

## 2. Ввод длины с клавиатуры

- [x] 2.1 Живое значение в поле: длина превью в выбранной единице, в метрах с долями; авто-фокус при клике по холсту; защита ручного ввода флагом `lengthDirty`; значение выделено целиком (`select()` на фокусе и после живой подстановки), поле `type="text" inputmode="decimal"`; очистка и blur при завершении цепочки (`src/main.ts`); проверить в браузере сценарии «Длина превью в поле», «Ввод заменяет значение целиком», «Ввод не затирается», «Очистка при завершении»
- [x] 2.2 Живое превью при вводе и коммит: превью введённой длины вдоль направления к курсору (`previewPoint`), единая `commitPoint` для Enter и клика (клик задаёт направление при активном вводе), парсинг запятой/точки, игнор некорректных значений, дефолтная единица мм; проверить в браузере по пикселям превью при вводе, точную длину по Enter и по клику, продолжение цепочки и live-значение после коммита

## 3. Итоговая проверка

- [x] 3.1 Прогнать все сценарии из `specs/length-input/spec.md` в dev-сервере; убедиться, что `npx vitest run` и `npm run build` проходят без ошибок; синхронизировать артефакты `add-wall-dimensions` с новым расположением переключателя единиц
