# Test Validation: stable-wall-drawing (TCR-2 — проверка правила приставленного квадрата)

Валидатор: Test Validator (fresh context, READ-ONLY). Дата: 2026-09-15. Точечная проверка TCR-2 (одобрен пользователем): зона прилипания к грани расширена до `reach = max(radiusCm, newHalf)`; дельта wall-drawing уточнена.

## Проверка по пунктам

| Пункт | Вердикт | Пересчёт / обоснование |
|---|---|---|
| (а) Новое правило vs «Радиус не зависит от толщины» | **Не нарушает** | `reach = max(radiusCm, newHalf)` в `wallCandidates` зависит от радиуса и полутолщины НОВОЙ стены (параметр `newWallThicknessCm`); толщина соседа входит только в геометрический `faceDist = \|lat\| − hW`, не в порог зоны. Дельта уточнена корректно: «НЕ ДОЛЖЕН зависеть от толщины существующих стен», «зона прилипания зависит только от радиуса и размера квадрата новой стены». Симметрия закреплена SNAP-RADIUS-1: 7 см — прилипание у соседей 40 и 10, 11 см — сетка у обеих |
| (б) Три теста ↔ реализация ↔ дельта | **Согласованы** | SNAP-FACE-2: 5.9 ≤ 10 → (50,10); 9.9 ≤ reach=10 → (50,10) (край квадрата достиг грани); 10.1 → grid (50,20). SNAP-RADIUS-1: 7 см → wall (50,20)/(250,5), 11 см → grid (50,30)/(250,20) у обеих толщин. SNAP-RADIUS-2: 8 см → (50,10) wall, 11 см → grid. Граница включительная (`faceDist <= reach`) соответствует «не больше половины толщины новой стены»; сценарий «Квадрат приставлен вплотную к грани — прилипание» и «Вне радиуса и за краем квадрата…» покрыты. Пересчёт существующих snap-тестов (END-1/2/3/4, CONT-1, PRIO, DET, BOUND) по новой логике `wallCandidates` — ожидания сохранились |
| (в) in-band-всегда-грань и приоритет торца vs дельта | **Не противоречат** | In-band: курсор на теле (расстояние 0) обязан прилипать — прежняя дыра «посреди тела → сетка» нарушала «стены → сетка»; теперь ближайшая грань всегда. Приоритет торца (dist до конца ≤ radius → конец оси) соответствует сценарию «Привязка к концу стены» («в пределах полосы — к торцу») и логике onJoint в main.ts. Замечания MINOR: (1) приоритет торца — ранний return в обход D1-правила «ближайший кандидат», тестом не закреплён (срабатывает только при hW соседа < радиуса); (2) in-band ветка продолжения за торцом действует на любом удалении — читается как «продолжение полосы» (расстояние до неё 0), коллизий с тестами нет |
| (г) Прогоны | **Зелёные** | `npx vitest run`: Test Files 6 passed (6), Tests **211 passed** (168 старых + 43 новых); `npx tsc --noEmit` — 0 ошибок |

## Дополнительно проверено

- **subtractBody** — послойное полосное разложение: каждая точка куска относится к первой исключающей плоскости, куски результата не пересекаются и не дублируются; пустые вырожденные куски отсеяны по площади. Класс дефекта «дубликаты кусков» ловится HIT-CONS-1 (`covered ≤ 1`, сетка 150 точек) и assertions о площадях/неприадлежности в JOINT-*; сам контур — render-уровень (Out of Scope), там `render.ts` теперь потребляет `contourSegments`/`displayPolygons`.
- **Wiring main.ts** — ограничение раунда 2 «no-op wiring» снято в коде: `updateWallCursor` вызывает `snapVertex(raw, walls, snapRadiusCm(view.zoom), GRID_STEP_CM, thicknessCm, chainStart && ortho ? chainStart : undefined)` (единый снаппер, радиус из чистой функции, орто на обе вершины), клик в зоне торца начинает цепочку вместо выделения (взаимодействие — вне юнит-скоупа, компенсация verify-change).
- MINOR (док): строки таблицы test-suite.md для SNAP-FACE-2/SNAP-RADIUS-1/SNAP-RADIUS-2 не обновлены под новые кейсы (9.9/10.1; 7/11; 8/11) — компенсируется примечанием TCR-2 в шапке файла.

## VERDICT: PASS

TCR-2 согласован во всех трёх слоях (реализация ↔ тесты ↔ дельта): правило приставленного квадрата устраняет реальный дефект (радиус 6 см < полтолщины квадрата), не ослабляя защиту «радиус не зависит от толщины существующих стен»; ни один из прежних защитных assertions не ослаблен (211/211 зелёные, tsc чист). Сьюта в целом — PASS; рекомендации MINOR (закрепить приоритет торца тестом при тонком соседе, обновить строки test-suite.md) — по желанию Test Writer, не блокируют. На verify-change: живая проверка цепочек «квадрат вплотную» и контура после subtractBody.

---

# Test Validation: stable-wall-drawing (раунд 3 — точечная перепроверка)

Валидатор: Test Validator (fresh context, READ-ONLY). Дата: 2026-09-15. Проверены 4 правки Test Writer по MAJOR/MINOR раунда 2; пересчёт геометрии выполнен вручную.

## Статус правок

| Правка (раунд 2) | Статус | Пересчёт |
|---|---|---|
| №1 MAJOR: JOINT-91-1 вакуумный | **Закрыт** | Фильтр `|x−50| ≤ 15 && 10.1 < y < 16` — строго зона стыка. Косая трактовка: вершина косого торца (59.9985, 10.1745) в фильтре (|9.9985|≤15, y∈(10.1,16)) → тест проходит. Ослабление допуска ≥1°: торец срезается заподлицо — все вершины стыка на y=10 ровно, дальний торец (y≈70) отсечён `y<16`, грань-пересечение (≈40,10) — тоже → фильтр пуст → мутант провален. Заявление test-plan теперь соответствует действительности |
| №2 MAJOR: SNAP-END-4 fatNew вне радиуса | **Закрыт** | thinNew (98,−24): gap = 24−20 = 4 ≤ R=6 ✓; проекция в пределах полосы → торец → (100−5, −20) = (95,−20) ✓. fatNew (75,−24): gap = 4 ≤ 6 ✓ → (100−30, −20) = (70,−20) ✓. Оба курсора в пределах радиуса при толщинно-независимой метрике — противоречие со спекой («радиус не зависит от толщины») устранено; ожидаемые точки соответствуют design D1 (полутолщина НОВОЙ стены от плоскости торца, грань заподлицо с соседом) |
| №3 MINOR: JOINT-OVERLAP-1 без guard'а | **Закрыт** | `length ≥ 1` добавлен; пустой результат больше не проходит вакуумно. Мутант «нет обрезки наложения» ловится: сырая вершина косого торца (40.0015, 9.8255) имеет lateral 9.83 < 10 → провал |
| №4 MINOR: завышенные claims Mutation Targets | **Закрыт** | Формулировка честная: верхняя сторона защищена (JOINT-91-1, механизм описан верно), нижняя объявлена юнит-невыразимой с указанием компенсации (render-уровень, Out of Scope). Замечание: «области совпадают» — строго говоря, при 90.3° остаётся клин-слэ шириной ~0.05 см (ниже порога 0.1 фильтра), но это не отменяет честности ограничения |

## Регрессии

- Перевод JOINT-OVERLAP-1 на tScene(60): сцена «поздняя ныряет сквозь тело ранней» (прежняя −30°) больше не тестируется на display-уровне; остаточное перекрытие для косого глубокого наложения не покрыто. Не блокирует: обрезка на уровне торца покрыта самой JOINT-OVERLAP-1 (60°), инвариант «нет наложений отображаемых полигонов» для перпендикулярного случая — HIT-CONS-1 (сетка 150 точек, `covered ≤ 1`). Фиксирую как наблюдение MINOR, не требующее правки.
- Противоречий между новыми и соседними тестами не найдено; SNAP-END-4 согласован со SNAP-END-1 (та же семантика зон конца).
- `npx vitest run`: `Test Files 1 failed | 5 passed (6)`, `Tests 168 passed (168)` — единственный fail по-прежнему отсутствие модуля `./wall-geometry` (ожидаемо до реализации).

## VERDICT: PASS

Оба MAJOR раунда 2 закрыты содержательно (проверено пересчётом координат, а не по формулировкам), оба MINOR закрыты. Ядро спеки защищено: допуск прямого угла сверху (91°→косой торец), радиус без зависимости от толщины (4 см от грани при любой паре толщин), орто в обе оси с фиксом гридинга, приоритеты по различающимся точкам, владелец клина при реверсе, угловой клин 45°, коллинеарность, плоские торцы 3+ концов, согласованность hit⇔полигоны (сетка 150 точек), чистота API (deep-freeze). Границы защиты честно задокументированы в test-plan (нижняя сторона допуска, шов, wiring main.ts — Out of Scope с компенсацией на apply/verify). Реализации запрещено менять тест-файл (Rule 4); на Apply/Verify проверить разводку main.ts процессно (verify-change), как зафиксировано в Out of Scope.

---

# Test Validation: stable-wall-drawing (раунд 2)

Валидатор: Test Validator (fresh context, READ-ONLY). Дата: 2026-09-15. Повторная независимая проверка после переработки тестов по отчёту раунда 1 (VERDICT: FAIL). Все выводы — чтением файлов, пересчётом геометрии и запуском тестов.

## Статус проблем раунда 1

| № | Проблема (раунд 1) | Статус | Чем закрыт / что осталось |
|---|---|---|---|
| 1 MAJOR | Нет теста вертикальной орто | **Закрыт** | SNAP-ORTHO-VERT-1: (7,103) от (0,0) → (0,100); без вертикальной ветви мутант дал бы (10,100) — убит. Дублирует защиту SNAP-ORTHO-FIXED-1 (вертикальный случай) |
| 2 MAJOR | Неподвижная координата при орто вне сетки | **Закрыт** | SNAP-ORTHO-FIXED-1: from (3,23) вне сетки → (100,23) точно; мутант «гридить обе» дал бы (100,20) — убит. Заодно убивает реальный дефект нынешнего `snap()` (geometry.ts:100–103) |
| 3 MAJOR | Верхняя граница допуска 0.5° (91°) | **НЕ закрыт** | JOINT-91-1 **вакуумный**: фильтр `p.y > 10.1` по всем вершинам всегда непуст — дальний торец стены (b≈(48.95,69.99), вершины y≈69.8/70.2) лежит выше 10.1 при любой трактовке стыка. Мутант «допуск 1°…29°» проходит. Детали — в «Новых проблемах» №1 |
| 4 MAJOR | Приоритет стены>орто только по source | **Закрыт** | SNAP-PRIO-2 переписан: (49,12.5) → стена (49,10) против орто+сетка (50,0) — точки различны, assertion по точке. Мутант «орто первым» убит |
| 5 MAJOR | Клин при угловом (торец-к-торцу) | **Закрыт** | JOINT-OBLIQUE-4 (45° от торца, старт (90,-10)): прямые assertions требуют залива выемки; реверс-assertion «(95,-9.5) НЕ в polylate» убивает и no-wedge, и «клин только в T-ветке» |
| 6 MAJOR | Wiring main.ts не защищён | **Закрыт (принятое ограничение)** | По варианту Б из раунда 1: SNAP-RADIUS-PX-1 фиксирует чистую функцию `snapRadiusCm` (12/(PX_PER_CM·zoom) — сверено с types.ts), остальное явно в Out of Scope test-plan с компенсацией verify-change + код-ревью |
| 7 MAJOR | Швы однотипных/разнотипных вне скоупа и таблиц | **Закрыт** | Out of Scope test-plan: шов — render-уровень, геометрия полигонов одинакова, защита — код-ревью на apply/verify; сценарии поименованы |
| 8 MAJOR | Толщина новой стены вне контракта | **Частично** | Контракт расширен (design D1: `newWallThicknessCm`, одобрено), SNAP-END-4 добавлен. Но кейс fatNew противоречит радиусной семантике спеки — см. «Новые проблемы» №2 |
| 9 MINOR | Коллинеарное отображение | **Закрыт** | JOINT-COLLIN-1: ранняя нетронута (area 2000), зазор 0.5 закрыт поздней ((100.2,5) покрыт), нет выхода из полосы граней |
| 10 MINOR | Вне-сеточная диагональ | **Закрыт** | SNAP-ORTHO-2: (103,41) → (100,40); сырый проход дал бы (103,41) — сетка на диагонали зафиксирована |
| 11 MINOR | Реверс массива в SNAP-DET-2 | **Закрыт** | Оба порядка: [first,second] → (50,10), [second,first] → (50,14) |
| 12 MINOR | Vacuous-циклы JOINT-RIGHT-2/3 | **Закрыт (с остатком)** | `length ≥ 1` добавлен в оба. Остаток MINOR: JOINT-OVERLAP-1 — тот же паттерн без guard'а (см. «Новые проблемы» №3) |
| 13 MINOR | Точные равенства границ | **Закрыт** | SNAP-ORTHO-5: ровно 15° (то же выражение tan(15°), включительно) притягивает, +0.1 — нет; SNAP-END-3: \|d\|=10 ровно → торец, 10.001 → угол |
| 14 MINOR | Плоский торец c в JOINT-MULTI-1 | **Закрыт** | `toContainEqual([90,8])`/`([110,8])` — плоский торец c зафиксирован; пропуски coverage-таблиц устранены в test-plan |

Также закрыт пункт 9 раунда (фактические ошибки test-plan): «91°→JOINT-OBLIQUE-1» и «T-стык→SNAP-FACE-2» исправлены, «результаты не совпадают» для SNAP-PRIO теперь соответствует содержимому.

## Запуск тестов (фактический вывод)

`npx vitest run src/wall-geometry.test.ts`:

```
FAIL  src/wall-geometry.test.ts
Error: Cannot find module './wall-geometry' imported from C:/Projects/draw-repair/src/wall-geometry.test.ts
Test Files  1 failed (1)   Tests  no tests
```

`npx vitest run`:

```
Test Files  1 failed | 5 passed (6)
Tests  168 passed (168)
```

Совпадает с test-suite.md: 43 теста падают до реализации (модуль создаётся на этапе Apply), существующие 168 не затронуты. Ожидаемо и правильно.

## Повторный adversarial-анализ

| Мутант | Результат | Обоснование |
|---|---|---|
| Удалённая вертикальная ветвь орто | **Не проходит** | SNAP-ORTHO-VERT-1: (7,103)→(0,100); мутант даёт grid(7,103)=(10,100) |
| Гриддинг обеих координат после орто | **Не проходит** | SNAP-ORTHO-FIXED-1: (103,27) от (3,23) → (100,23); мутант даёт (100,20) |
| Ослабление допуска 0.5° → 1°…29° | **Проходит** | JOINT-91-1 вакуумный (см. ниже). Реально верхняя граница допуска фиксируется только JOINT-OBLIQUE-4 при ≥~45° (45°-сцена классифицировалась бы как прямой угол и реверс-assertion падает); диапазон 1°…44° не защищён |
| Клин только в T-ветке (угловой не заливается) | **Не проходит** | JOINT-OBLIQUE-4: без залива (101,-10.2)/(99,-10.2) не покрыты; реверс: (95,-9.5) остался бы в полигоне 45°-стены |
| Коллинеарность → клин | Проходит (безвредно) | Вырожденный клин между коллинеарными геометрически неотличим от прямоугольника; JOINT-COLLIN-1 фиксирует наблюдаемый контракт (зазор сведён, без выхода из полосы) — поведение соответствует спеке |
| Приоритет орто → стены | **Не проходит** | SNAP-PRIO-2: орто-первым дал бы (50,0) ≠ (49,10) |
| Приоритет стены>орто по source-only | **Не проходит** | Assertion по точке (49,10), не по source |
| No-op wiring в main.ts | Проходит | Осознанное ограничение: контракт в тестах, разводка — verify-change (Out of Scope). Текущий main.ts по-прежнему вызывает `findVertexSnap(raw, walls, thicknessCm/2)` и орто в мёртвой ветке — закрыть процессно на Apply/Verify |
| Срезанный торец c в JOINT-MULTI-1 | **Не проходит** | Точные вершины [90,8]/[110,8] плоского торца c |
| (доп.) Клин, добавленный при 90.3° | Проходит | JOINT-RIGHT-2/3 проверяют только габарит (вершины клина лежат на гранях поздней, lateral ≤ 10) — нижняя граница допуска фактически не защищена; claim test-plan «[0.3°, 1°)» завышен |

## Проверка новых тестов на вакуумность и корректность геометрии

Пересчитано вручную (полигоны, клинья, принадлежность точек):

- **JOINT-91-1 — ВАКУУМНЫЙ.** `latePolys.flat().filter((p) => p.y > 10.1)` всегда непуст: дальний торец сцены (b≈(48.95, 69.99)) даёт вершины y≈69.8 и 70.2 при любой трактовке стыка, включая «прямоугольную» (торец заподлицо, y=10). Тест не отличает косую трактовку от прямой; утверждение test-plan «ослабление допуска до ≥1° проваливает тест» фактически неверно. Достаточно ограничить фильтр зоной стыка (как в JOINT-OBLIQUE-1: `p.y > 10.1 && p.y < 16`) — при косой трактовке вершина (59.999, 10.17) попадает, при заподлицо (y=10) — нет.
- **JOINT-OBLIQUE-2** — корректен и невакуумен: (42,14.5) лежит в треугольнике клина (38.45,10)-(41.34,15)-(50,10) (пересчёт: при y=14.5 клин покрывает x∈[41.05,42.21]); (58,8) внутри тела ранней — обрезка поздней проверяется содержательно; реверс согласован.
- **JOINT-OBLIQUE-4** — корректен: (99,-10.2) и (101,-10.2) лежат в полосе поздней ниже грани ранней (lateral 6.2/7.6 < 10), требуя залива выемки; реверс-assertion требует обрезки внутри тела ранней. Метрика близости (дистанция до тела) выполняется.
- **JOINT-COLLIN-1, SNAP-ORTHO-VERT-1, SNAP-ORTHO-FIXED-1, SNAP-ORTHO-5, SNAP-END-3, SNAP-RADIUS-PX-1, JOINT-MULTI-1** — невакуумны, геометрия сценариев сходится, ожидаемые точки соответствуют контракту D1/D4.
- **SNAP-END-4 (thinNew)** — на границе радиуса (gap ровно 6 ≤ R), допустимо; **fatNew — некорректен** (см. ниже).
- Метрика близости во всех snap-тестах согласована: расстояние от курсора до тела/полосы стены (bodyGap), для CONT-1 — до продолжения полосы; противоречий с «расстояние от курсора» не найдено, кроме fatNew.

## Новые проблемы

1. **MAJOR — JOINT-91-1 вакуумный, верхняя граница допуска 0.5° не защищена (проблема раунда 1 №3 не закрыта).** Фильтр по всем вершинам ловит дальний торец, а не зону стыка. Мутант «допуск 1°…29°» проходит все 43 теста; стыки 1–29° рендерились бы как прямые — прямое нарушение сценария дельты «Наклон за допуском — клин (91°)». Нужно: ограничить фильтр зоной стыка (например `p.y > 10.1 && p.y < 16`).
2. **MAJOR — SNAP-END-4 fatNew противоречит спецификации.** Курсор (90,-30) при R=6 находится вне радиуса от стены (0,0,100,0,40) при любой толщинно-независимой метрике: до тела 10 (lateral 30 − h 20), до плоскости торца 10, до угла (100,-20) 14.1. Спека: «в пределах радиуса привязки», «радиус НЕ ДОЛЖЕН зависеть от толщины»; design D4: радиус не читает толщину. Реализация, честно следующая контракту, этот assertion провалит; тест вынуждает толщинно-зависимый радиус. Нужно: вход в пределах радиуса (например (94,-26) → (70,-20)) либо явное обоснование в test-plan.
3. **MINOR — JOINT-OVERLAP-1 без guard'а непустоты** (`length ≥ 1`): при пустом результате цикл проходит вакуумно для сцены tScene(-30). Остаток раунда 1 №12; содержательно мутант «нет обрезки наложения» всё же ловится (вершины внутри полосы ранней).
4. **MINOR — test-plan Mutation Targets содержит два завышенных утверждения:** «JOINT-91-1: ослабление допуска до ≥1° проваливает тест» (не проваливает — вакуумный) и «тесты ограничивают допуск интервалом [0.3°, 1°)» (фактически защищено только «< ~45°» через JOINT-OBLIQUE-4; сторона 0.3–1° и нижняя граница 0.3° не зафиксированы). После починки №1 ограничение сверху станет честным; нижнюю сторону либо зафиксировать содержательно (assert прямоугольности при 90.3°: например площадь поздней = площадь прямоугольника), либо честно описать в Out of Scope.

## VERDICT: FAIL

Сьют существенно усилен: 12 из 14 проблем раунда 1 закрыты содержательно (не формально), ядро (радиус в обе стороны, орто в обе оси с фиксом гридинга, приоритеты по точкам, владелец клина при реверсе, угловой клин 45°, коллинеарность, плоские торцы 3-х концов, согласованность hit⇔полигоны, чистота) защищено хорошо. Возвращается на этап Test Writing из-за двух MAJOR:

1. JOINT-91-1 переписать с фильтром по зоне стыка — иначе допуск прямого угла сверху не защищён (сценарий дельты «Наклон за допуском — клин, 91°» остаётся без защиты).
2. SNAP-END-4 fatNew: заменить вход на пределах радиуса — сейчас тест противоречит спеке («радиус не зависит от толщины») и не может быть пройден реализацией, честно следующей контракту.

Плюс MINOR №3 (guard в JOINT-OVERLAP-1) и №4 (исправить завышенные claims в Mutation Targets test-plan.md). Исправлять тесты самостоятельно запрещено (Rule 3). Отдельно на Apply/Verify: no-op wiring main.ts по-прежнему зелёный — проверить разводку процессно (verify-change), как зафиксировано в Out of Scope.
