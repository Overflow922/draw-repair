## 1. Типы и миграция

- [x] 1.1 `src/types.ts`: `MATERIALS` (5 id/label) вместо `WALL_TYPES`, `normalizeMaterial` (валидные id проходят, остальное → `brick`); обновить `types.test.ts` (legacy partition/drywall/bearing/selfbearing/nonbearing → brick; валидные проходят)
- [x] 1.2 `src/storage.ts`: `normalizeMaterial` в `parseStore` (оба формата); обновить тесты миграции в `storage.test.ts`

## 2. Паттерны по ГОСТ 2.306-68

- [x] 2.1 `src/render.ts`: перевод отрисовки стены в экранные координаты (полигон → CSS-px, клип), контур 2px, выделение/превью в той же системе; линейные штриховки 45° с шагом в экранных мм (кирпич — сплошные, бетон — штрихпунктир, ж/б — чередование)
- [x] 2.2 `src/render.ts`: паттерн дерева (вдоль) — волнистые линии по оси под клипом полигона; торец древесины удалён по решению пользователя (вместе с `wood-cross` из `MATERIALS` и тестов)

## 3. UI и логика

- [x] 3.1 `index.html` / `src/main.ts` / `src/style.css`: 4 свотча-легенды (мини-канвас + подпись материала), дефолт `brick`, обработчики клика и стрелок по `MATERIALS`, CSS-градиенты свотчей удалены
- [x] 3.2 `index.html` / `src/style.css`: выпадающий список `#render-style` в нижней панели с единственным пунктом «ГОСТ» (каркас переключателя, без JS)

## 4. Верификация

- [x] 4.1 В браузере: 4 стены с разными материалами различимы; подписи-легенда у свотчей; линейные штриховки строго 45° на наклонных стенах и при зуме; шаг на экране постоянен при зуме; смена материала меняет только выделенную; legacy localStorage → «Кирпич»; свотчи-превью соответствуют паттернам
- [x] 4.2 `npm test` и `npm run build` — зелёные
