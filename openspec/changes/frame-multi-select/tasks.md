## 1. Геометрия

- [x] 1.1 Добавить `moveWalls(walls, group, delta)` в `geometry.ts`: сдвиг всех стен группы одним вектором, затем один проход тяг по невыдленным (стыки концов + `axisAttached`, примыкание к двум выделенным сдвигается ровно один раз); покрыть юнит-тестами в `geometry.test.ts` (группа без соединений, стык с невыдленной, T-примыкание к двум выделенным — один вектор, изоляция от стен вне группы). Проверка: `npx vitest run src/geometry.test.ts` зелёный.

## 2. Рендер

- [x] 2.1 Перевести `render`/`drawScene` на `selectedWalls: Wall[]`, `opts.selectedDims: Dimension[]`, добавить `opts.marquee` (пунктирный прямоугольник в экранных координатах); маркеры стен и размеров — только при длине списка 1; поправить вызов в `export/pdf.ts` (пустые списки). Проверка: `npx tsc --noEmit` без ошибок.

## 3. Выделение и жест рамки

- [x] 3.1 Заменить `selectedWall`/`selectedDimension` на `selectedWalls`/`selectedDimensions` во всех обработчиках `main.ts` (панели и ручки — при одиночном, обводка — всегда, сворачивание смешанного выделения при прямом взаимодействии); проверить `setTool`, `activate`, `resetEditing`, `undo`/`redo`, каскадное удаление. Проверка: `npx tsc --noEmit`, ручная проверка одиночного выделения не регрессировала.
- [x] 3.2 Реализовать жест рамки: `marqueePending` в `pointerdown` (ЛКМ по пустому, кроме ластика) с pointer capture, порог 5 px, рисование через `opts.marquee`, выделение при `pointerup` (полное попадание: оба конца стены / вся размерная линия; Shift — добавить), `suppressClick` после рамки. Проверка: `npx tsc --noEmit`; браузером — сценарии «Рамка выделяет…», «Рамка не начинает цепочку», «Точка замера не задаётся», «Ластик не рисует рамку» из спеки.
- [x] 3.3 Правка по обратной связи: попадание рамки изменено с полного охвата на пересечение — `segmentIntersectsRect` (Лианг–Барски) в `geometry.ts` + тесты, использование в `finishMarquee` (стена — с запасом полутолщины, размер — по линии); обновлены proposal/design/spec. Проверка: `npx tsc --noEmit`, `npx vitest run`; браузером — частично задетая стена и размер выделяются, незадетые нет.
- [x] 3.4 Правка по обратной связи: живая подсветка объектов, задетых рамкой при протягивании — расчёт `marqueePicks` в каждом `pointermove`, передача `opts.marqueeHits` в рендер, мягкие цвета подсветки стен и размеров; обновлены design/spec. Проверка: `npx tsc --noEmit`; браузером — подсветка следует за рамкой, после отпускания сменяется выделением.
- [x] 3.5 Правка по обратной связи «выделяется больше чем нужно»: убран запас полутолщины — стена выделяется только при пересечении рамкой её осевой линии (касание края — нет); обновлены design/spec. Проверка: `npx tsc --noEmit`, `npx vitest run`; браузером — касание края не выделяет, перекрытие до оси — выделяет.

## 4. Групповые операции

- [x] 4.1 Групповой драг стен по образцу `wallMove`: захват тела выделенной стены тянет всю группу через `moveWalls`, снап опорного конца против стен вне группы и не примкнутых к ней, `cloneScene` в `pointerdown`, `recordSnapshot` в `pointerup` при сдвиге; захват невыдленной стены сворачивает выделение к ней. Проверка: браузером сценарии «Две выделенные стены…», «T-примыкание сдвигается один раз», «Перемещение без сдвига не пишет историю», «Размеры следуют за группой».
- [x] 4.2 Сдвиг стрелками: шаг сетки 10 см, Shift — 1 см, burst-слияние истории (флаг `nudgeBurst`, сброс в местах `record`/`recordSnapshot`, `undo`/`redo`); no-op при выделенных только размерах; стрелки при непустом выделении не переключают материал; игнор при фокусе в полях ввода. Проверка: браузером сценарии «Стрелка сдвигает группу…», «Серия нажатий — одна запись», «Другое действие завершает серию», «Выделенные стены не переключают материал стрелками».
- [x] 4.3 Групповое удаление Delete: одна запись истории, удаление всех выделенных стен + каскад размеров + выделенных размеров, очистка выделения. Проверка: браузером сценарии «Delete при мультивыделении…», «Undo возвращает всё удалённое мультивыделение», Delete в поле ввода не удаляет.

## 5. Верификация

- [x] 5.1 Прогнать полный набор: `npx tsc --noEmit`, `npx vitest run`, `npm run build`; пройти сценарии изменённых дельт (`wall-selection`, `wall-deletion`, `wall-types`) и убедиться, что одиночные режимы и экспорт PDF работают как прежде; оставить дев-сервер запущенным и дать ссылку на страницу.
