## 1. Спайк шима context2d

- [x] 1.1 Скриптом/страницей проверить `doc.context2d` jsPDF на: `setLineDash` (штрих-пунктир бетона/ж/б), clip по полигону, `translate`/`rotate`, повёрнутый `fillText`; открыть полученный PDF и зафиксировать вердикт — шим или план Б (свой слой операторов, design D2) — **вердикт: шим годен** (все проверенные операции корректны); пространство шима канвасоподобное (y вниз, origin сверху-слева), ручной флип оси Y не нужен

## 2. Подготовка

- [x] 2.1 Добавить зависимость `jspdf`; `npm install` и `npm run build` проходят
- [x] 2.2 Добавить кириллический OFL TTF-шрифт ассетом; проверка загрузки в jsPDF (`addFileToVFS`/`addFont`) на спайк-странице из 1.1 — PT Sans Regular (OFL, ParaType), `src/assets/pt-sans-regular.ttf`; кириллица и повёрнутый текст с встроенным шрифтом проверены визуально

## 3. Рендер

- [x] 3.1 Параметризовать метрики рендера в `src/render.ts` (умолчания = текущие экранные) и добавить флаг отключения сетки; `npm test` зелёный, экран визуально не изменился — 77 тестов зелёные, в браузере стены/штриховки/подписи/сетка без изменений

## 4. Модуль экспорта

- [x] 4.1 Чистая функция подгонки страницы (bbox + формат → масштаб/ориентация/смещение) + юнит-тесты (все форматы A0–A4, альбом/портрет, поля); `npm test`
- [x] 4.2 Имя файла: санитизация `\/:*?"<>|` + метка времени `ГГГГ-ММ-ДД_ЧЧ-мм-сс` + юнит-тесты (включая «Чертёж 1_2026-09-05_14-32-07.pdf»); `npm test`
- [x] 4.3 `exportToPdf`: jsPDF с выбранным форматом, перевернутая ось Y, метрики страницы из 3.1, встроенный шрифт для подписей, скачивание через Blob; smoke-тест вывода (начинается с `%PDF`, содержит встроенный шрифт); `npm test` — флип оси не потребовался (шаг 1.1), `buildPdf` + `exportDrawing` в `src/export/pdf.ts`; 91 тест зелёный, tsc/build зелёные

## 5. Интерфейс

- [x] 5.1 В нижнюю панель `index.html` добавить select формата (A4 по умолчанию, A0–A4) и кнопку экспорта; в `main.ts` — проводка: активная вкладка, блокировка кнопки при пустом чертеже, скачивание

## 6. Верификация

- [x] 6.1 Прогнать сценарии спеки в браузере: экспорт A3/A4, альбомная ориентация, кириллица в подписях, штрих-пунктир бетона в PDF, санитизация имени, пустая вкладка — кнопка неактивна; `npm run build` и `npm test` зелёные — 92 теста зелёные, tsc/build зелёные; A3 ландшафт выверен по content stream (поля ≥10мм, флип Y, шаг штриховки 0.25мм, шрифт встроен) и визуально; санитизация «Отчёт: бетон/стены?» → «Отчёт бетонстены_2026-09-05_18-33-19.pdf»; пустая вкладка — кнопка неактивна
